<nav class="bg-white/80 backdrop-blur-lg border-b border-gray-200/80 sticky top-0 z-30">
  <div class="px-3 sm:px-6">
    <div class="flex items-center justify-between h-14 sm:h-16">
      <div class="flex items-center gap-2 sm:gap-3">
        <% if (user) { %>
        <button id="sidebar-toggle" class="lg:hidden p-2 -ml-1 text-gray-500 hover:text-brand-600 hover:bg-brand-50 rounded-xl focus:outline-none active:scale-95 transition-all" aria-label="Toggle menu">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
        </button>
        <% } %>
        <a href="/dashboard" class="flex items-center gap-2 sm:gap-3">
          <img src="/images/logo-colour.jpg" alt="T&S" class="h-8 sm:h-10 w-auto rounded-lg shadow-sm">
          <div class="hidden sm:block">
            <span class="text-gray-900 font-bold text-base sm:text-lg leading-tight block">Operations Dashboard</span>
          </div>
        </a>
      </div>
      <div class="flex items-center gap-1.5 sm:gap-3">
        <% if (user) { %>
          <!-- Notification Bell + Popup -->
          <div class="relative" id="notif-container">
            <button onclick="toggleNotifDropdown()" class="relative p-2.5 text-gray-400 hover:text-brand-600 transition-all focus:outline-none rounded-xl hover:bg-brand-50 active:scale-95" title="Notifications" id="notif-bell">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/></svg>
              <% if (typeof unreadNotifications !== 'undefined' && unreadNotifications > 0) { %>
                <span id="notif-badge" class="absolute top-1.5 right-1.5 w-4 h-4 bg-red-500 text-white text-[10px] font-bold rounded-full flex items-center justify-center animate-pulse"><%= unreadNotifications > 9 ? '9+' : unreadNotifications %></span>
              <% } else { %>
                <span id="notif-badge" class="absolute top-1.5 right-1.5 w-4 h-4 bg-red-500 text-white text-[10px] font-bold rounded-full flex items-center justify-center hidden">0</span>
              <% } %>
            </button>

            <!-- Dropdown Popup -->
            <div id="notif-dropdown" class="hidden absolute right-0 mt-2 w-[calc(100vw-2rem)] sm:w-80 max-w-sm bg-white border border-gray-200 rounded-2xl shadow-2xl z-50 overflow-hidden" style="min-height:120px">
              <div class="flex items-center justify-between px-4 py-3 border-b border-gray-100 bg-gray-50/50">
                <span class="font-bold text-sm text-gray-900">Notifications</span>
                <span id="notif-unread-label" class="text-xs text-gray-400"></span>
              </div>
              <div id="notif-list" class="max-h-80 overflow-y-auto">
                <div class="p-6 text-center text-gray-400 text-sm" id="notif-loading">Loading...</div>
              </div>
              <div class="flex items-center justify-between px-4 py-2.5 border-t border-gray-100 bg-gray-50/50">
                <button onclick="markAllReadFromPopup()" class="text-xs font-medium text-gray-500 hover:text-brand-600 transition">Mark All Read</button>
                <a href="/notifications" class="text-xs font-bold text-brand-600 hover:text-brand-700 transition">View All &rarr;</a>
              </div>
            </div>
          </div>

          <!-- Quick Add Menu -->
          <div class="relative" id="quickadd-container">
            <button onclick="toggleQuickAdd()" class="p-2.5 text-gray-400 hover:text-brand-600 transition-all focus:outline-none rounded-xl hover:bg-brand-50 active:scale-95" title="Quick Create" id="quickadd-btn">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/></svg>
            </button>
            <div id="quickadd-dropdown" class="hidden absolute right-0 mt-2 w-56 bg-white border border-gray-200 rounded-2xl shadow-2xl z-50 overflow-hidden">
              <div class="px-4 py-2.5 border-b border-gray-100 bg-gray-50/50">
                <span class="font-bold text-sm text-gray-900">Quick Create</span>
              </div>
              <div class="py-1.5">
                <a href="/jobs/new" class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-brand-50 hover:text-brand-700 transition-colors">
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/></svg>
                  New Job
                </a>
                <a href="/tasks/new" class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-brand-50 hover:text-brand-700 transition-colors">
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/></svg>
                  New Task
                </a>
                <a href="/crew/new" class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-brand-50 hover:text-brand-700 transition-colors">
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"/></svg>
                  Add Crew Member
                </a>
                <a href="/timesheets/new" class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-brand-50 hover:text-brand-700 transition-colors">
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  Log Timesheet
                </a>
                <div class="border-t border-gray-100 my-1"></div>
                <a href="/incidents/new" class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-red-50 hover:text-red-700 transition-colors">
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z"/></svg>
                  Report Incident
                </a>
                <a href="/defects/new" class="flex items-center gap-3 px-4 py-2 text-sm text-gray-700 hover:bg-amber-50 hover:text-amber-700 transition-colors">
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  Report Defect
                </a>
              </div>
            </div>
          </div>

          <!-- Profile avatar (clickable) -->
          <a href="/profile" class="flex items-center gap-2 hover:opacity-80 transition-all active:scale-95" title="My Profile">
            <span class="text-gray-600 text-sm font-medium hidden lg:inline"><%= user.full_name %></span>
            <div class="w-9 h-9 sm:w-8 sm:h-8 rounded-xl bg-gradient-to-br from-brand-500 to-brand-700 text-white flex items-center justify-center text-xs font-bold shadow-sm">
              <%= user.full_name ? user.full_name.split(' ').map(n => n[0]).join('').toUpperCase().substring(0, 2) : 'U' %>
            </div>
          </a>

          <span class="hidden lg:inline-flex items-center px-2.5 py-1 rounded-lg text-xs font-semibold
            <% if (user.role === 'admin') { %>bg-purple-50 text-purple-700 ring-1 ring-purple-600/20
            <% } else if (user.role === 'operations') { %>bg-blue-50 text-blue-700 ring-1 ring-blue-600/20
            <% } else if (user.role === 'planning') { %>bg-teal-50 text-teal-700 ring-1 ring-teal-600/20
            <% } else if (user.role === 'finance') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
            <% } %>">
            <%= user.role.charAt(0).toUpperCase() + user.role.slice(1) %>
          </span>
          <a href="/logout" class="hidden lg:inline-flex items-center gap-1.5 text-gray-400 hover:text-red-500 text-sm transition-colors px-2 py-1 rounded-lg hover:bg-red-50">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>
            Logout
          </a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<% if (user) { %>
<script>
(function() {
  let notifOpen = false;
  const dropdown = document.getElementById('notif-dropdown');
  const container = document.getElementById('notif-container');

  window.toggleNotifDropdown = function() {
    if (notifOpen) {
      dropdown.classList.add('hidden');
      notifOpen = false;
    } else {
      dropdown.classList.remove('hidden');
      notifOpen = true;
      loadNotifications();
    }
  };

  // Close on click outside
  document.addEventListener('click', function(e) {
    if (notifOpen && !container.contains(e.target)) {
      dropdown.classList.add('hidden');
      notifOpen = false;
    }
  });

  function timeAgo(dateStr) {
    const now = new Date();
    const d = new Date(dateStr);
    const diffMs = now - d;
    const mins = Math.floor(diffMs / 60000);
    if (mins < 1) return 'just now';
    if (mins < 60) return mins + 'm ago';
    const hrs = Math.floor(mins / 60);
    if (hrs < 24) return hrs + 'h ago';
    const days = Math.floor(hrs / 24);
    if (days < 7) return days + 'd ago';
    return d.toLocaleDateString('en-AU', { day: 'numeric', month: 'short' });
  }

  function typeColor(type) {
    if (['overdue_task','new_incident','critical_defect'].includes(type)) return 'bg-red-500';
    if (['expiring_compliance','equipment_overdue','ticket_expiry'].includes(type)) return 'bg-amber-500';
    if (type === 'missing_update') return 'bg-yellow-400';
    if (['corrective_action_due'].includes(type)) return 'bg-rose-500';
    if (type === 'follow_up_due') return 'bg-purple-500';
    if (type === 'over_budget') return 'bg-orange-500';
    return 'bg-brand-500';
  }

  function loadNotifications() {
    const list = document.getElementById('notif-list');
    list.innerHTML = '<div class="p-6 text-center text-gray-400 text-sm">Loading...</div>';

    fetch('/notifications/recent')
      .then(function(r) { return r.json(); })
      .then(function(data) {
        const badge = document.getElementById('notif-badge');
        const label = document.getElementById('notif-unread-label');
        if (data.unreadCount > 0) {
          badge.textContent = data.unreadCount > 9 ? '9+' : data.unreadCount;
          badge.classList.remove('hidden');
          label.textContent = data.unreadCount + ' unread';
        } else {
          badge.classList.add('hidden');
          label.textContent = 'All caught up';
        }

        if (data.notifications.length === 0) {
          list.innerHTML = '<div class="p-6 text-center"><svg class="w-8 h-8 text-gray-300 mx-auto mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg><p class="text-gray-400 text-sm">No new notifications</p></div>';
          return;
        }

        var html = '';
        data.notifications.forEach(function(n) {
          var dotClass = typeColor(n.type);
          html += '<form method="POST" action="/notifications/' + n.id + '/read" class="block">';
          html += '<button type="submit" class="w-full text-left px-4 py-3 hover:bg-gray-50 transition-colors border-b border-gray-50 flex items-start gap-3">';
          html += '<span class="mt-1.5 w-2 h-2 rounded-full flex-shrink-0 ' + dotClass + '"></span>';
          html += '<div class="flex-1 min-w-0">';
          html += '<p class="text-sm font-medium text-gray-900 truncate">' + n.title.replace(/</g, '&lt;') + '</p>';
          html += '<p class="text-xs text-gray-400 mt-0.5 truncate">' + n.message.replace(/</g, '&lt;') + '</p>';
          html += '<span class="text-[10px] text-gray-400 mt-0.5 block">' + timeAgo(n.created_at) + '</span>';
          html += '</div></button></form>';
        });
        list.innerHTML = html;
      })
      .catch(function() {
        list.innerHTML = '<div class="p-6 text-center text-red-400 text-sm">Failed to load</div>';
      });
  }

  window.markAllReadFromPopup = function() {
    fetch('/notifications/mark-all-read', { method: 'POST', headers: { 'Content-Type': 'application/x-www-form-urlencoded' } })
      .then(function() {
        loadNotifications();
        var badge = document.getElementById('notif-badge');
        badge.classList.add('hidden');
      })
      .catch(function() {});
  };

  // Quick Add dropdown
  var qaOpen = false;
  var qaDropdown = document.getElementById('quickadd-dropdown');
  var qaContainer = document.getElementById('quickadd-container');
  window.toggleQuickAdd = function() {
    if (qaOpen) { qaDropdown.classList.add('hidden'); qaOpen = false; }
    else { qaDropdown.classList.remove('hidden'); qaOpen = true; if (notifOpen) { dropdown.classList.add('hidden'); notifOpen = false; } }
  };
  document.addEventListener('click', function(e) {
    if (qaOpen && !qaContainer.contains(e.target)) { qaDropdown.classList.add('hidden'); qaOpen = false; }
  });
})();
</script>
<% } %>
