<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= moduleTitle %> — Presenter</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');

    * { margin: 0; padding: 0; box-sizing: border-box; }

    html, body {
      width: 100vw;
      height: 100vh;
      overflow: hidden;
      font-family: 'Inter', system-ui, -apple-system, sans-serif;
      background: #ffffff;
      color: #1a1a2e;
      cursor: none;
      user-select: none;
      -webkit-user-select: none;
    }

    body.cursor-visible { cursor: default; }

    /* ── Brand Palette ── */
    :root {
      --ts-navy: #1e3a5f;
      --ts-navy-dark: #152a45;
      --ts-cyan: #06B6D4;
      --ts-cyan-light: #22d3ee;
      --ts-orange: #F97316;
      --ts-green: #16A34A;
      --ts-blue: #2563EB;
      --ts-teal: #0D9488;
      --ts-purple: #7C3AED;
      --ts-red: #DC2626;
      --ts-gray-50: #f9fafb;
      --ts-gray-100: #f3f4f6;
      --ts-gray-200: #e5e7eb;
      --ts-gray-300: #d1d5db;
      --ts-gray-500: #6b7280;
      --ts-gray-700: #374151;
      --ts-gray-900: #111827;
    }

    /* ── Slide Stage ── */
    #stage {
      position: relative;
      width: 100vw;
      height: 100vh;
      overflow: hidden;
    }

    .slide {
      position: absolute;
      inset: 0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 80px 100px;
      opacity: 0;
      transform: translateX(40px);
      transition: opacity 0.5s cubic-bezier(0.16, 1, 0.3, 1),
                  transform 0.5s cubic-bezier(0.16, 1, 0.3, 1);
      pointer-events: none;
      will-change: opacity, transform;
      background: #ffffff;
    }

    .slide.active {
      opacity: 1;
      transform: translateX(0);
      pointer-events: auto;
    }

    .slide.exit-left {
      opacity: 0;
      transform: translateX(-40px);
    }

    /* ── Cyan Accent Bars (brand element on left edge) ── */
    .slide::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 6px;
      background: var(--ts-cyan);
      z-index: 2;
    }

    .slide::after {
      content: '';
      position: absolute;
      left: 0;
      bottom: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--ts-cyan), var(--ts-cyan-light), transparent 60%);
      z-index: 2;
    }

    /* Three vertical cyan bars brand mark */
    .slide .brand-bars {
      position: absolute;
      left: 20px;
      top: 50%;
      transform: translateY(-50%);
      display: flex;
      gap: 4px;
      z-index: 3;
    }

    .slide .brand-bars span {
      width: 4px;
      height: 40px;
      background: var(--ts-cyan);
      border-radius: 2px;
    }

    /* ── Layout: Title ── */
    .slide.layout-title {
      justify-content: center;
      align-items: flex-start;
      text-align: left;
      padding-left: 120px;
      padding-right: 100px;
      background: #ffffff;
    }

    .slide.layout-title::before {
      width: 8px;
      background: var(--ts-navy);
    }

    .slide.layout-title .title-accent {
      position: absolute;
      right: 0;
      top: 0;
      bottom: 0;
      width: 35%;
      background: linear-gradient(135deg, var(--ts-navy) 0%, var(--ts-navy-dark) 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 1;
    }

    .slide.layout-title .title-accent .accent-logo {
      color: rgba(255, 255, 255, 0.9);
      font-size: 2.5rem;
      font-weight: 800;
      letter-spacing: 0.05em;
    }

    .slide.layout-title .title-accent .accent-bars {
      position: absolute;
      left: -20px;
      top: 50%;
      transform: translateY(-50%);
      display: flex;
      gap: 5px;
    }

    .slide.layout-title .title-accent .accent-bars span {
      width: 5px;
      height: 60px;
      background: var(--ts-cyan);
      border-radius: 3px;
    }

    .slide.layout-title .slide-icon {
      font-size: 3.5rem;
      margin-bottom: 1.5rem;
      animation: float 6s ease-in-out infinite;
    }

    .slide.layout-title h1 {
      font-size: clamp(2.5rem, 5vw, 4rem);
      font-weight: 800;
      letter-spacing: -0.03em;
      line-height: 1.1;
      color: var(--ts-gray-900);
      margin-bottom: 1.5rem;
      max-width: 55%;
      position: relative;
      z-index: 2;
    }

    .slide.layout-title .subtitle {
      font-size: clamp(1.1rem, 2vw, 1.5rem);
      font-weight: 400;
      color: var(--ts-gray-500);
      max-width: 50%;
      line-height: 1.6;
      position: relative;
      z-index: 2;
    }

    .slide.layout-title .logo-mark {
      position: absolute;
      bottom: 60px;
      left: 120px;
      font-size: 0.85rem;
      font-weight: 700;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--ts-cyan);
      z-index: 2;
    }

    /* ── Layout: Content ── */
    .slide.layout-content {
      justify-content: flex-start;
      align-items: flex-start;
      padding: 90px 100px 80px 120px;
      background: #ffffff;
    }

    .slide.layout-content h2 {
      font-size: clamp(1.8rem, 3.5vw, 2.8rem);
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 0.5rem;
      color: var(--ts-gray-900);
    }

    .slide.layout-content .slide-subtitle {
      font-size: 1.1rem;
      color: var(--ts-gray-500);
      font-weight: 400;
      margin-bottom: 2.5rem;
    }

    .slide.layout-content .content-body {
      font-size: clamp(1rem, 1.5vw, 1.2rem);
      line-height: 1.9;
      color: var(--ts-gray-700);
      max-width: 900px;
    }

    .slide.layout-content .content-body p { margin-bottom: 1.25rem; }
    .slide.layout-content .content-body strong { color: var(--ts-blue); font-weight: 600; }

    /* ── Layout: Split ── */
    .slide.layout-split {
      padding: 90px 80px 80px 120px;
      justify-content: flex-start;
      align-items: flex-start;
      background: #ffffff;
    }

    .slide.layout-split h2 {
      font-size: clamp(1.8rem, 3vw, 2.5rem);
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 0.5rem;
      width: 100%;
      color: var(--ts-gray-900);
    }

    .slide.layout-split .slide-subtitle {
      font-size: 1.1rem;
      color: var(--ts-gray-500);
      margin-bottom: 2.5rem;
      width: 100%;
    }

    .slide.layout-split .split-columns {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1.5rem;
      width: 100%;
      flex: 1;
    }

    .slide.layout-split .split-col {
      font-size: 1.05rem;
      line-height: 1.8;
      color: #ffffff;
      padding: 2rem 2.25rem;
      background: var(--ts-blue);
      border-radius: 16px;
      border: none;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
    }

    .slide.layout-split .split-col:nth-child(1) { background: var(--ts-blue); }
    .slide.layout-split .split-col:nth-child(2) { background: var(--ts-teal); }
    .slide.layout-split .split-col:nth-child(3) { background: var(--ts-orange); }
    .slide.layout-split .split-col:nth-child(4) { background: var(--ts-purple); }
    .slide.layout-split .split-col:nth-child(5) { background: var(--ts-green); }
    .slide.layout-split .split-col:nth-child(6) { background: var(--ts-red); }

    .slide.layout-split .split-col strong { color: rgba(255, 255, 255, 0.95); font-weight: 700; }

    .slide.layout-split .split-col h3,
    .slide.layout-split .split-col h4 {
      color: #ffffff;
      font-weight: 700;
      margin-bottom: 0.75rem;
      font-size: 1.2rem;
    }

    /* ── Layout: List ── */
    .slide.layout-list {
      justify-content: flex-start;
      align-items: flex-start;
      padding: 90px 100px 80px 120px;
      background: #ffffff;
    }

    .slide.layout-list h2 {
      font-size: clamp(1.8rem, 3.5vw, 2.8rem);
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 0.5rem;
      color: var(--ts-gray-900);
    }

    .slide.layout-list .slide-subtitle {
      font-size: 1.1rem;
      color: var(--ts-gray-500);
      margin-bottom: 2.5rem;
    }

    .slide.layout-list .content-body {
      width: 100%;
      max-width: 1000px;
    }

    .slide.layout-list .content-body ul,
    .slide.layout-list .content-body ol {
      list-style: none;
      padding: 0;
    }

    .slide.layout-list .content-body li {
      position: relative;
      padding: 1rem 1.5rem 1rem 1.5rem;
      margin-bottom: 0.6rem;
      font-size: clamp(1rem, 1.4vw, 1.15rem);
      line-height: 1.6;
      color: #ffffff;
      background: var(--ts-blue);
      border-radius: 12px;
      border: none;
      transition: transform 0.3s ease;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
    }

    .slide.layout-list .content-body li:nth-child(1) { background: var(--ts-blue); }
    .slide.layout-list .content-body li:nth-child(2) { background: var(--ts-teal); }
    .slide.layout-list .content-body li:nth-child(3) { background: var(--ts-orange); }
    .slide.layout-list .content-body li:nth-child(4) { background: var(--ts-green); }
    .slide.layout-list .content-body li:nth-child(5) { background: var(--ts-purple); }
    .slide.layout-list .content-body li:nth-child(6) { background: var(--ts-red); }
    .slide.layout-list .content-body li:nth-child(7) { background: var(--ts-navy); }
    .slide.layout-list .content-body li:nth-child(8) { background: var(--ts-blue); }
    .slide.layout-list .content-body li:nth-child(9) { background: var(--ts-teal); }
    .slide.layout-list .content-body li:nth-child(10) { background: var(--ts-orange); }

    .slide.layout-list .content-body li::before {
      display: none;
    }

    .slide.layout-list .content-body li strong { color: rgba(255, 255, 255, 0.95); font-weight: 700; }

    /* ── Layout: Highlight ── */
    .slide.layout-highlight {
      justify-content: center;
      align-items: center;
      text-align: center;
      background: var(--ts-gray-50);
    }

    .slide.layout-highlight .highlight-card {
      max-width: 800px;
      padding: 3.5rem 4rem;
      background: #ffffff;
      border: 2px solid var(--ts-cyan);
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.06),
                  0 0 0 4px rgba(6, 182, 212, 0.08);
    }

    .slide.layout-highlight .slide-icon {
      font-size: 3rem;
      margin-bottom: 1.5rem;
    }

    .slide.layout-highlight h2 {
      font-size: clamp(1.6rem, 3vw, 2.4rem);
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 1.5rem;
      color: var(--ts-gray-900);
    }

    .slide.layout-highlight .content-body {
      font-size: clamp(1.05rem, 1.5vw, 1.25rem);
      line-height: 1.8;
      color: var(--ts-gray-700);
    }

    .slide.layout-highlight .content-body strong { color: var(--ts-cyan); font-weight: 600; }

    /* ── Layout: Quiz ── */
    .slide.layout-quiz {
      justify-content: center;
      align-items: center;
      text-align: center;
      background: #ffffff;
    }

    .slide.layout-quiz .quiz-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 1.25rem;
      background: var(--ts-orange);
      border: none;
      border-radius: 100px;
      font-size: 0.85rem;
      font-weight: 600;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: #ffffff;
      margin-bottom: 2rem;
      box-shadow: 0 2px 8px rgba(249, 115, 22, 0.3);
    }

    .slide.layout-quiz h2 {
      font-size: clamp(1.8rem, 3.5vw, 2.8rem);
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 2rem;
      max-width: 850px;
      line-height: 1.3;
      color: var(--ts-gray-900);
    }

    .slide.layout-quiz .content-body {
      max-width: 800px;
      font-size: clamp(1.05rem, 1.4vw, 1.2rem);
      line-height: 1.8;
      color: var(--ts-gray-700);
      text-align: left;
    }

    .slide.layout-quiz .content-body p { margin-bottom: 1rem; }
    .slide.layout-quiz .content-body strong { color: var(--ts-blue); font-weight: 600; }

    /* ── Top Bar ── */
    #top-bar {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 1.5rem;
      height: 48px;
      background: var(--ts-navy);
      border-bottom: 2px solid var(--ts-cyan);
      opacity: 0;
      transform: translateY(-10px);
      transition: opacity 0.35s ease, transform 0.35s ease;
      pointer-events: none;
    }

    #top-bar.visible {
      opacity: 1;
      transform: translateY(0);
      pointer-events: auto;
    }

    #top-bar .logo {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      font-weight: 700;
      font-size: 0.9rem;
      color: rgba(255, 255, 255, 0.9);
    }

    #top-bar .logo-icon {
      width: 30px;
      height: 30px;
      background: var(--ts-cyan);
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 800;
      font-size: 0.65rem;
      color: var(--ts-navy);
      letter-spacing: 0.02em;
    }

    #top-bar .module-name {
      font-weight: 400;
      color: rgba(255, 255, 255, 0.5);
      margin-left: 0.5rem;
    }

    .top-btn {
      display: inline-flex;
      align-items: center;
      gap: 0.4rem;
      padding: 0.4rem 0.9rem;
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 6px;
      background: rgba(255, 255, 255, 0.08);
      color: rgba(255, 255, 255, 0.8);
      font-size: 0.78rem;
      font-weight: 500;
      font-family: inherit;
      cursor: pointer;
      transition: all 0.2s ease;
      text-decoration: none;
    }

    .top-btn:hover {
      background: rgba(255, 255, 255, 0.15);
      color: #fff;
      border-color: rgba(255, 255, 255, 0.3);
    }

    /* ── Bottom Bar ── */
    #bottom-bar {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      z-index: 100;
      padding: 0;
      background: var(--ts-gray-900);
    }

    #progress-track {
      width: 100%;
      height: 3px;
      background: rgba(255, 255, 255, 0.08);
    }

    #progress-fill {
      height: 100%;
      background: var(--ts-cyan);
      border-radius: 0 3px 3px 0;
      transition: width 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    }

    #bottom-info {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.5rem 1.5rem;
    }

    #slide-counter {
      font-size: 0.85rem;
      font-weight: 500;
      color: rgba(255, 255, 255, 0.4);
      font-variant-numeric: tabular-nums;
      min-width: 80px;
    }

    #slide-counter .current {
      color: var(--ts-cyan);
      font-weight: 700;
      font-size: 1rem;
      display: inline-block;
      transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    }

    #slide-counter .current.bump {
      transform: scale(1.3);
    }

    #bottom-module {
      font-size: 0.8rem;
      color: rgba(255, 255, 255, 0.3);
      font-weight: 400;
    }

    #bottom-keys {
      font-size: 0.75rem;
      color: rgba(255, 255, 255, 0.2);
      display: flex;
      gap: 0.75rem;
    }

    #bottom-keys kbd {
      padding: 0.15rem 0.45rem;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 4px;
      font-family: inherit;
      font-size: 0.7rem;
    }

    /* ── Click Zones ── */
    #click-prev, #click-next {
      position: fixed;
      top: 0;
      bottom: 60px;
      z-index: 50;
      cursor: none;
    }

    body.cursor-visible #click-prev { cursor: w-resize; }
    body.cursor-visible #click-next { cursor: e-resize; }

    #click-prev { left: 0; width: 30%; }
    #click-next { right: 0; width: 70%; }

    /* ── Animations ── */
    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .animate-in { animation: fadeInUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) both; }
    .delay-1 { animation-delay: 0.1s; }
    .delay-2 { animation-delay: 0.2s; }
    .delay-3 { animation-delay: 0.3s; }

    /* ── Responsive ── */
    @media (max-width: 768px) {
      .slide { padding: 60px 30px 60px 50px; }
      .slide.layout-title { padding-left: 50px; }
      .slide.layout-title h1 { max-width: 90%; }
      .slide.layout-title .subtitle { max-width: 85%; }
      .slide.layout-title .title-accent { display: none; }
      .slide.layout-split { padding: 60px 30px 60px 50px; }
      .slide.layout-split .split-columns { grid-template-columns: 1fr; gap: 1rem; }
      .slide.layout-highlight .highlight-card { padding: 2rem 2.5rem; }
      #bottom-keys { display: none; }
      .slide .brand-bars { left: 14px; }
    }
  </style>
</head>
<body class="cursor-visible">

  <!-- Top Bar -->
  <div id="top-bar">
    <div class="logo">
      <div class="logo-icon">T&S</div>
      <span>T&S Operations</span>
      <span class="module-name">— <%= moduleTitle %></span>
    </div>
    <div style="display:flex;gap:0.5rem;">
      <button class="top-btn" onclick="toggleFullscreen()">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M8 3H5a2 2 0 0 0-2 2v3m18 0V5a2 2 0 0 0-2-2h-3m0 18h3a2 2 0 0 0 2-2v-3M3 16v3a2 2 0 0 0 2 2h3"/></svg>
        <span id="fs-label">Fullscreen</span>
      </button>
      <a href="/induction/admin/presentations" class="top-btn">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
        Exit
      </a>
    </div>
  </div>

  <!-- Stage -->
  <div id="stage"></div>

  <!-- Click Zones -->
  <div id="click-prev" onclick="prevSlide()"></div>
  <div id="click-next" onclick="nextSlide()"></div>

  <!-- Bottom Bar -->
  <div id="bottom-bar">
    <div id="progress-track">
      <div id="progress-fill" style="width: 0%"></div>
    </div>
    <div id="bottom-info">
      <div id="slide-counter">
        <span class="current">1</span> / <span id="total-count"><%= totalSlides %></span>
      </div>
      <div id="bottom-module"><%= moduleTitle %></div>
      <div id="bottom-keys">
        <span><kbd>←</kbd> <kbd>→</kbd> Navigate</span>
        <span><kbd>F</kbd> Fullscreen</span>
        <span><kbd>Esc</kbd> Exit</span>
      </div>
    </div>
  </div>

  <script>
    // ── Data ──
    const slides = <%- JSON.stringify(slides) %>;
    const totalSlides = <%= totalSlides %>;
    let currentIndex = 0;
    let isAnimating = false;

    // ── Build Slides ──
    const stage = document.getElementById('stage');

    function buildSlide(slide, index) {
      const el = document.createElement('div');
      el.className = 'slide layout-' + (slide.layout || 'content');
      el.dataset.index = index;
      if (index === 0) el.classList.add('active');

      let html = '';

      // Brand bars (three cyan vertical bars)
      html += '<div class="brand-bars"><span></span><span></span><span></span></div>';

      switch (slide.layout) {
        case 'title':
          html += '<div class="title-accent"><div class="accent-bars"><span></span><span></span><span></span></div><div class="accent-logo">T&S</div></div>';
          if (slide.icon) html += '<div class="slide-icon animate-in">' + slide.icon + '</div>';
          html += '<h1 class="animate-in delay-1">' + (slide.title || '') + '</h1>';
          if (slide.subtitle) html += '<div class="subtitle animate-in delay-2">' + slide.subtitle + '</div>';
          html += '<div class="logo-mark animate-in delay-3">T&S OPERATIONS</div>';
          break;

        case 'split':
          html += '<h2 class="animate-in">' + (slide.title || '') + '</h2>';
          if (slide.subtitle) html += '<div class="slide-subtitle animate-in delay-1">' + slide.subtitle + '</div>';
          html += '<div class="split-columns animate-in delay-2">' + (slide.content || '') + '</div>';
          break;

        case 'list':
          if (slide.icon) html += '<div class="slide-icon animate-in" style="font-size:2rem;margin-bottom:0.5rem;align-self:flex-start;">' + slide.icon + '</div>';
          html += '<h2 class="animate-in delay-1">' + (slide.title || '') + '</h2>';
          if (slide.subtitle) html += '<div class="slide-subtitle animate-in delay-1">' + slide.subtitle + '</div>';
          html += '<div class="content-body animate-in delay-2">' + (slide.content || '') + '</div>';
          break;

        case 'highlight':
          html += '<div class="highlight-card">';
          if (slide.icon) html += '<div class="slide-icon animate-in">' + slide.icon + '</div>';
          html += '<h2 class="animate-in delay-1">' + (slide.title || '') + '</h2>';
          html += '<div class="content-body animate-in delay-2">' + (slide.content || '') + '</div>';
          html += '</div>';
          break;

        case 'quiz':
          html += '<div class="quiz-badge animate-in">&#9889; Knowledge Check</div>';
          html += '<h2 class="animate-in delay-1">' + (slide.title || '') + '</h2>';
          html += '<div class="content-body animate-in delay-2">' + (slide.content || '') + '</div>';
          break;

        default: // content
          if (slide.icon) html += '<div style="font-size:2rem;margin-bottom:0.5rem;align-self:flex-start;" class="animate-in">' + slide.icon + '</div>';
          html += '<h2 class="animate-in delay-1">' + (slide.title || '') + '</h2>';
          if (slide.subtitle) html += '<div class="slide-subtitle animate-in delay-1">' + slide.subtitle + '</div>';
          html += '<div class="content-body animate-in delay-2">' + (slide.content || '') + '</div>';
          break;
      }

      el.innerHTML = html;
      return el;
    }

    slides.forEach(function(slide, i) {
      stage.appendChild(buildSlide(slide, i));
    });

    // ── Navigation ──
    function goToSlide(index, direction) {
      if (isAnimating || index < 0 || index >= totalSlides || index === currentIndex) return;
      isAnimating = true;

      const allSlides = stage.querySelectorAll('.slide');
      const current = allSlides[currentIndex];
      const next = allSlides[index];
      const goingForward = index > currentIndex;

      // Set initial position for incoming slide
      next.style.transition = 'none';
      next.style.opacity = '0';
      next.style.transform = goingForward ? 'translateX(40px)' : 'translateX(-40px)';

      // Force reflow
      void next.offsetWidth;

      // Re-enable transitions
      next.style.transition = '';

      // Exit current
      current.classList.remove('active');
      current.classList.add(goingForward ? 'exit-left' : '');
      if (!goingForward) {
        current.style.transform = 'translateX(40px)';
        current.style.opacity = '0';
      }

      // Enter next
      next.classList.add('active');
      next.classList.remove('exit-left');
      next.style.opacity = '1';
      next.style.transform = 'translateX(0)';

      currentIndex = index;
      updateUI();

      setTimeout(function() {
        isAnimating = false;
        // Clean up
        allSlides.forEach(function(s, i) {
          if (i !== currentIndex) {
            s.classList.remove('active', 'exit-left');
            s.style.transition = 'none';
            s.style.opacity = '0';
            s.style.transform = 'translateX(40px)';
            void s.offsetWidth;
            s.style.transition = '';
          }
        });
      }, 550);
    }

    function nextSlide() { goToSlide(currentIndex + 1); }
    function prevSlide() { goToSlide(currentIndex - 1); }

    function updateUI() {
      // Counter
      const counterEl = document.querySelector('#slide-counter .current');
      counterEl.textContent = currentIndex + 1;
      counterEl.classList.add('bump');
      setTimeout(function() { counterEl.classList.remove('bump'); }, 300);

      // Progress
      var pct = totalSlides > 1 ? ((currentIndex) / (totalSlides - 1)) * 100 : 100;
      document.getElementById('progress-fill').style.width = pct + '%';
    }

    // ── Keyboard ──
    document.addEventListener('keydown', function(e) {
      switch(e.key) {
        case 'ArrowRight':
        case ' ':
          e.preventDefault();
          nextSlide();
          break;
        case 'ArrowLeft':
          e.preventDefault();
          prevSlide();
          break;
        case 'Escape':
          window.location.href = '/induction/admin/presentations';
          break;
        case 'f':
        case 'F':
          e.preventDefault();
          toggleFullscreen();
          break;
      }
    });

    // ── Touch / Swipe ──
    let touchStartX = 0;
    let touchStartY = 0;

    document.addEventListener('touchstart', function(e) {
      touchStartX = e.changedTouches[0].screenX;
      touchStartY = e.changedTouches[0].screenY;
    }, { passive: true });

    document.addEventListener('touchend', function(e) {
      const dx = e.changedTouches[0].screenX - touchStartX;
      const dy = e.changedTouches[0].screenY - touchStartY;
      if (Math.abs(dx) > Math.abs(dy) && Math.abs(dx) > 50) {
        if (dx < 0) nextSlide();
        else prevSlide();
      }
    }, { passive: true });

    // ── Fullscreen ──
    function toggleFullscreen() {
      if (!document.fullscreenElement) {
        document.documentElement.requestFullscreen().catch(function() {});
      } else {
        document.exitFullscreen().catch(function() {});
      }
    }

    document.addEventListener('fullscreenchange', function() {
      var label = document.getElementById('fs-label');
      label.textContent = document.fullscreenElement ? 'Exit Fullscreen' : 'Fullscreen';
    });

    // ── Top Bar Hover ──
    let hideTimer;
    const topBar = document.getElementById('top-bar');

    document.addEventListener('mousemove', function(e) {
      document.body.classList.add('cursor-visible');
      clearTimeout(hideTimer);

      if (e.clientY < 80) {
        topBar.classList.add('visible');
      } else {
        topBar.classList.remove('visible');
      }

      hideTimer = setTimeout(function() {
        document.body.classList.remove('cursor-visible');
        topBar.classList.remove('visible');
      }, 3000);
    });

    // ── Init ──
    updateUI();
  </script>
</body>
</html>