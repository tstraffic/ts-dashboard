<div class="max-w-2xl mx-auto space-y-6">

  <!-- Header -->
  <div>
    <h1 class="text-2xl font-bold text-gray-900">My Profile</h1>
    <p class="text-sm text-gray-500 mt-1">Update your personal information and security settings</p>
  </div>

  <%# Flash renders via the layout banner (global locals). %>

  <!-- Worker portal access (only when this admin is linked to a roster profile) -->
  <% if (typeof workerLink !== 'undefined' && workerLink) { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-5 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
      <div class="flex items-start gap-3">
        <div class="w-10 h-10 rounded-lg bg-emerald-100 flex items-center justify-center flex-shrink-0">
          <svg class="w-5 h-5 text-emerald-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 17v-2a4 4 0 014-4h0a4 4 0 014 4v2M7 7a4 4 0 108 0 4 4 0 00-8 0z"/></svg>
        </div>
        <div>
          <h2 class="text-base font-semibold text-gray-900">Worker portal</h2>
          <p class="text-sm text-gray-500 mt-0.5">Your admin account is linked to roster profile <strong><%= workerLink.full_name %></strong> (<%= workerLink.employee_id %>). Open the crew portal with this login — no separate PIN needed.</p>
        </div>
      </div>
      <a href="/profile/worker-portal" class="inline-flex items-center justify-center px-4 py-2.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition whitespace-nowrap">
        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M14 5l7 7m0 0l-7 7m7-7H3"/></svg>
        Open worker portal
      </a>
    </div>
  <% } %>

  <!-- Profile Info Card -->
  <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-hidden">
    <div class="px-5 py-4 border-b border-gray-200 bg-gray-50">
      <h2 class="text-base font-semibold text-gray-900">Personal Information</h2>
    </div>
    <form method="POST" action="/profile" class="p-5 space-y-5">
      <%- include('partials/csrf') %>
      <!-- Avatar / initials -->
      <div class="flex items-center gap-4">
        <div class="w-16 h-16 rounded-full bg-brand-100 text-brand-700 flex items-center justify-center text-xl font-bold flex-shrink-0">
          <%= profile.full_name.split(' ').map(n => n[0]).join('').toUpperCase().substring(0, 2) %>
        </div>
        <div>
          <p class="text-lg font-semibold text-gray-900"><%= profile.full_name %></p>
          <p class="text-sm text-gray-500">@<%= profile.username %> &middot;
            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
              <% if (profile.role === 'admin') { %>bg-purple-50 text-purple-700 ring-1 ring-purple-600/20
              <% } else if (profile.role === 'operations') { %>bg-blue-50 text-blue-700 ring-1 ring-blue-600/20
              <% } else if (profile.role === 'planning') { %>bg-teal-50 text-teal-700 ring-1 ring-teal-600/20
              <% } else if (profile.role === 'finance') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
              <% } else { %>bg-gray-50 text-gray-700 ring-1 ring-gray-600/20
              <% } %>">
              <%= profile.role.charAt(0).toUpperCase() + profile.role.slice(1) %>
            </span>
          </p>
          <p class="text-xs text-gray-400 mt-0.5">Member since <%= new Date(profile.created_at).toLocaleDateString('en-AU', { timeZone: 'Australia/Sydney', month: 'long', year: 'numeric' }) %></p>
        </div>
      </div>

      <hr class="border-gray-200">

      <!-- Full Name -->
      <div>
        <label for="full_name" class="block text-sm font-medium text-gray-700 mb-1">Full Name <span class="text-red-500">*</span></label>
        <input type="text" id="full_name" name="full_name" value="<%= profile.full_name %>" required
          class="w-full px-3 py-2.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
      </div>

      <!-- Email -->
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email Address</label>
        <input type="email" id="email" name="email" value="<%= profile.email || '' %>" placeholder="your@email.com"
          class="w-full px-3 py-2.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
        <p class="text-xs text-gray-400 mt-1">Used for notifications and password reset emails</p>
      </div>

      <!-- Username (read-only) -->
      <div>
        <label class="block text-sm font-medium text-gray-700 mb-1">Username</label>
        <input type="text" value="<%= profile.username %>" disabled
          class="w-full px-3 py-2.5 bg-gray-50 border border-gray-200 rounded-lg text-gray-500 text-sm cursor-not-allowed">
        <p class="text-xs text-gray-400 mt-1">Username cannot be changed. Contact an admin if needed.</p>
      </div>

      <!-- Notification Preferences -->
      <hr class="border-gray-200">
      <h3 class="text-sm font-semibold text-gray-900">Notification Preferences</h3>

      <div class="flex items-center gap-3">
        <label class="relative inline-flex items-center cursor-pointer">
          <input type="checkbox" name="email_notifications_enabled" class="sr-only peer" <%= profile.email_notifications_enabled ? 'checked' : '' %>>
          <div class="w-10 h-5 bg-gray-200 peer-focus:outline-none peer-focus:ring-2 peer-focus:ring-brand-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-4 after:w-4 after:transition-all peer-checked:bg-brand-600"></div>
        </label>
        <span class="text-sm text-gray-700">Email notifications</span>
      </div>

      <div>
        <label for="notification_frequency" class="block text-sm font-medium text-gray-700 mb-1">Notification Frequency</label>
        <select id="notification_frequency" name="notification_frequency"
          class="w-full px-3 py-2.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          <option value="immediate" <%= profile.notification_frequency === 'immediate' ? 'selected' : '' %>>Immediate</option>
          <option value="hourly" <%= profile.notification_frequency === 'hourly' ? 'selected' : '' %>>Hourly digest</option>
          <option value="daily" <%= profile.notification_frequency === 'daily' ? 'selected' : '' %>>Daily digest</option>
        </select>
        <p class="text-xs text-gray-400 mt-1">The master switch above and this cadence apply to whatever you choose to be emailed below.</p>
      </div>

      <!-- Per-category preferences: which notifications you get in-app, and
           which are also emailed. -->
      <% if (typeof notifCategories !== 'undefined' && notifCategories && notifCategories.length) { %>
        <input type="hidden" name="pref_submitted" value="1">
        <div>
          <p class="block text-sm font-medium text-gray-700 mb-1">What you get notified about</p>
          <div class="border border-gray-200 rounded-lg overflow-hidden">
            <div class="grid grid-cols-[1fr_auto_auto] gap-x-4 items-center px-4 py-2 bg-gray-50 border-b border-gray-200 text-[11px] font-bold uppercase tracking-wider text-gray-400">
              <span>Notification</span>
              <span class="w-14 text-center" title="Show in the app and as a push">In app</span>
              <span class="w-14 text-center" title="Also send me an email">Email</span>
            </div>
            <% notifCategories.forEach(function (c, i) { %>
              <div class="grid grid-cols-[1fr_auto_auto] gap-x-4 items-center px-4 py-2.5 <%= i % 2 ? 'bg-gray-50/40' : '' %> <%= i ? 'border-t border-gray-100' : '' %>">
                <div class="min-w-0">
                  <p class="text-sm font-medium text-gray-800"><%= c.label %></p>
                  <p class="text-xs text-gray-400"><%= c.desc %></p>
                </div>
                <label class="w-14 flex justify-center cursor-pointer">
                  <input type="checkbox" name="pref_<%= c.key %>_inapp" class="rounded border-gray-300 text-brand-600 focus:ring-brand-500 w-4 h-4" <%= c.inApp ? 'checked' : '' %>>
                </label>
                <label class="w-14 flex justify-center cursor-pointer">
                  <input type="checkbox" name="pref_<%= c.key %>_email" class="rounded border-gray-300 text-brand-600 focus:ring-brand-500 w-4 h-4" <%= c.email ? 'checked' : '' %>>
                </label>
              </div>
            <% }); %>
          </div>
          <p class="text-xs text-gray-400 mt-1">Unticking <span class="font-semibold">In app</span> stops that kind of notification entirely; <span class="font-semibold">Email</span> also sends it to your inbox.</p>
        </div>
      <% } %>

      <div class="pt-2">
        <button type="submit" class="w-full sm:w-auto px-6 py-2.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-semibold rounded-lg transition-colors">
          Save Changes
        </button>
      </div>
    </form>
  </div>

  <!-- Appearance / Theme Card -->
  <%
    // [id, label, swatch page background, swatch card style]
    const themeGroups = [
      { heading: 'Dark', items: [
        ['ts-dark',      'T&S Dark',      'background:radial-gradient(120% 80% at 50% -10%,rgba(61,49,192,0.35),transparent 60%),#100F1A', 'background:#1A1826;border:1px solid rgba(160,153,235,0.35)'],
        ['slate',        'Slate',         'background:#0F1115',  'background:rgba(255,255,255,0.07)'],
        ['carbon',       'Carbon',        'background:#0a0d12',  'background:#14181f'],
        ['navy',         'Midnight Navy', 'background:#0d1424',  'background:#141d33'],
        ['graphite',     'Graphite',      'background:#2a2f37',  'background:#343b45'],
        ['atomis',       'Atomis',        'background:radial-gradient(120% 80% at 50% -10%,rgba(16,185,129,0.30),transparent 60%),#0F1115', 'background:rgba(255,255,255,0.06);border:1px solid rgba(16,185,129,0.45)'],
      ]},
      { heading: 'Gradient', items: [
        ['aurora',       'Aurora',        'background:radial-gradient(80% 60% at 18% 0%,rgba(45,212,191,0.55),transparent 60%),radial-gradient(80% 60% at 80% 0%,rgba(217,70,239,0.4),transparent 60%),radial-gradient(90% 60% at 50% 0,rgba(139,92,246,0.5),transparent 62%),#0a0e1a', 'background:rgba(18,22,32,0.92)'],
        ['emerald-dusk', 'Emerald Dusk',  'background:linear-gradient(180deg,#064e3b,#070b09 75%)', 'background:rgba(15,22,18,0.92);border:1px solid rgba(16,185,129,0.4)'],
        ['violet-haze',  'Violet Haze',   'background:radial-gradient(100% 70% at 50% 0,rgba(124,58,237,0.7),transparent 65%),#121019', 'background:rgba(24,21,33,0.92)'],
        ['twilight',     'Twilight',      'background:linear-gradient(135deg,rgba(124,58,237,0.75),transparent 55%),linear-gradient(315deg,rgba(16,185,129,0.6),transparent 55%),#0c0a13', 'background:rgba(20,18,28,0.92)'],
        ['nebula',       'Nebula',        'background:radial-gradient(70% 60% at 25% 0,rgba(79,70,229,0.7),transparent 60%),radial-gradient(70% 60% at 75% 10%,rgba(147,51,234,0.6),transparent 60%),radial-gradient(60% 50% at 92% 22%,rgba(244,63,94,0.45),transparent 58%),#07070e', 'background:rgba(14,14,22,0.94)'],
      ]},
      { heading: 'Light', items: [
        ['ts',           'T&S',           'background:#F7F5F1',   'background:#ffffff;border:1px solid #E7E2D9'],
        ['daylight',     'Daylight',      'background:#F7F8FA',   'background:#ffffff;border:1px solid #E5E7EB'],
        ['warm-paper',   'Warm Paper',    'background:#faf8f4',   'background:#ffffff;border:1px solid #ECE7DE'],
        ['mint-glass',   'Mint Glass',    'background:linear-gradient(170deg,#e3f5ec,#ffffff 75%)', 'background:rgba(255,255,255,0.78);border:1px solid rgba(16,185,129,0.25)'],
      ]},
    ];
    const activeTheme = (typeof currentTheme !== 'undefined' && currentTheme) ? currentTheme : '';
  %>
  <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-hidden">
    <div class="px-5 py-4 border-b border-gray-200 bg-gray-50 flex items-start justify-between gap-3">
      <div>
        <h2 class="text-base font-semibold text-gray-900">Appearance</h2>
        <p class="text-xs text-gray-500 mt-0.5">Pick <strong>one light</strong> theme and <strong>one dark or gradient</strong> theme. The sun/moon button in the top bar switches between them.</p>
      </div>
      <span id="theme-current-label" class="text-xs font-medium text-gray-400 whitespace-nowrap flex-shrink-0"></span>
    </div>
    <div class="p-5 space-y-5">
      <% themeGroups.forEach(function (g) { %>
        <div>
          <p class="text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-2"><%= g.heading %></p>
          <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
            <% g.items.forEach(function (it) { %>
              <button type="button" class="theme-swatch group text-left rounded-xl border border-gray-200 p-2 transition hover:border-brand-400 hover:shadow-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-brand-500"
                      data-theme-id="<%= it[0] %>" <%= activeTheme === it[0] ? 'data-active="1"' : '' %>>
                <span class="block h-16 rounded-lg relative overflow-hidden ring-1 ring-black/5" style="<%= it[2] %>">
                  <span class="absolute left-2 right-6 top-2 h-3.5 rounded" style="<%= it[3] %>"></span>
                  <span class="absolute left-2 bottom-2 w-7 h-2.5 rounded" style="<%= it[3] %>"></span>
                  <span class="absolute bottom-2 right-2 flex gap-1">
                    <span class="w-1.5 h-1.5 rounded-full" style="background:#3D31C0"></span>
                    <span class="w-1.5 h-1.5 rounded-full" style="background:#FBBF24"></span>
                  </span>
                  <span class="theme-now absolute top-1.5 left-1.5 hidden items-center gap-1 px-1.5 py-0.5 rounded-md bg-black/55 text-white text-[9px] font-bold uppercase tracking-wide">
                    <span class="w-1.5 h-1.5 rounded-full bg-emerald-400"></span>Showing
                  </span>
                  <span class="theme-check absolute inset-0 hidden items-center justify-center bg-black/30">
                    <span class="w-6 h-6 rounded-full bg-brand-600 flex items-center justify-center">
                      <svg class="w-3.5 h-3.5 text-white" fill="none" stroke="currentColor" stroke-width="3" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
                    </span>
                  </span>
                </span>
                <span class="flex items-center justify-between mt-2 px-0.5">
                  <span class="text-xs font-semibold text-gray-700"><%= it[1] %></span>
                </span>
              </button>
            <% }); %>
          </div>
        </div>
      <% }); %>
    </div>
  </div>

  <script>
    (function () {
      var swatches = Array.prototype.slice.call(document.querySelectorAll('.theme-swatch'));
      if (!swatches.length) return;
      var labelEl = document.getElementById('theme-current-label');
      var csrf = (document.querySelector('meta[name="csrf-token"]') || {}).content || '';
      var THEMES = window.ATOMIS_THEMES || {};
      function ls(k) { try { return localStorage.getItem(k); } catch (e) { return null; } }
      function norm(id, fallback) {
        if (id === 'light') id = 'daylight';
        if (id === 'dark') id = 'slate';
        return THEMES[id] ? id : fallback;
      }
      function modeOf(id) { return (THEMES[id] || {}).mode || 'dark'; }

      // Two persistent picks: one per mode. The header toggle flips between
      // them. Fall back to the current theme for its own mode, else baselines.
      function picks() {
        var current = norm((window.currentAtomisTheme && window.currentAtomisTheme()) || 'slate', 'slate');
        var dark  = norm(ls('atomis-theme-dark'),  modeOf(current) === 'dark'  ? current : 'slate');
        var light = norm(ls('atomis-theme-light'), modeOf(current) === 'light' ? current : 'daylight');
        return { current: current, dark: dark, light: light };
      }

      function paint() {
        var p = picks();
        swatches.forEach(function (b) {
          var id = b.getAttribute('data-theme-id');
          var selected = (id === p.dark || id === p.light);
          var showing = (id === p.current);
          b.classList.toggle('border-brand-500', selected);
          b.classList.toggle('ring-2', selected);
          b.classList.toggle('ring-brand-500', selected);
          var chk = b.querySelector('.theme-check');
          if (chk) { chk.classList.toggle('hidden', !selected); chk.classList.toggle('flex', selected); }
          var now = b.querySelector('.theme-now');
          if (now) { now.classList.toggle('hidden', !showing); now.classList.toggle('flex', showing); }
        });
        if (labelEl) {
          var lt = (THEMES[p.light] || {}).label || '—';
          var dt = (THEMES[p.dark] || {}).label || '—';
          labelEl.innerHTML = 'Light: <span class="text-gray-600">' + lt + '</span> · Dark: <span class="text-gray-600">' + dt + '</span>';
        }
      }
      paint();

      swatches.forEach(function (btn) {
        btn.addEventListener('click', function () {
          var id = btn.getAttribute('data-theme-id');
          if (window.setAtomisTheme) window.setAtomisTheme(id); // apply + localStorage (per-mode too)
          paint();
          fetch('/profile/theme', {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'X-CSRF-Token': csrf, 'Accept': 'application/json' },
            body: 'theme=' + encodeURIComponent(id) + '&_csrf=' + encodeURIComponent(csrf)
          }).catch(function () { /* non-fatal — still applied locally */ });
        });
      });

      // Keep the picker in sync if the header sun/moon toggle is used.
      document.addEventListener('atomis:theme', paint);
    })();
  </script>

  <!-- Security Card -->
  <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-hidden">
    <div class="px-5 py-4 border-b border-gray-200 bg-gray-50">
      <h2 class="text-base font-semibold text-gray-900">Security</h2>
    </div>
    <div class="p-5 space-y-6">

      <!-- Change Password -->
      <div>
        <h3 class="text-sm font-semibold text-gray-900 mb-3">Change Password</h3>
        <form method="POST" action="/profile/change-password" class="space-y-4">
          <%- include('partials/csrf') %>
          <div>
            <label for="current_password" class="block text-sm font-medium text-gray-700 mb-1">Current Password</label>
            <input type="password" id="current_password" name="current_password" required autocomplete="current-password"
              class="w-full px-3 py-2.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div>
              <label for="new_password" class="block text-sm font-medium text-gray-700 mb-1">New Password</label>
              <input type="password" id="new_password" name="new_password" required minlength="8" autocomplete="new-password"
                class="w-full px-3 py-2.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
            </div>
            <div>
              <label for="confirm_password" class="block text-sm font-medium text-gray-700 mb-1">Confirm New Password</label>
              <input type="password" id="confirm_password" name="confirm_password" required minlength="8" autocomplete="new-password"
                class="w-full px-3 py-2.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
            </div>
          </div>
          <p class="text-xs text-gray-400">Minimum 8 characters</p>
          <button type="submit" class="w-full sm:w-auto px-6 py-2.5 bg-gray-800 hover:bg-gray-700 text-white text-sm font-semibold rounded-lg transition-colors">
            Update Password
          </button>
        </form>
      </div>

      <% if (emailEnabled && profile.email) { %>
      <hr class="border-gray-200">

      <!-- Reset via Email -->
      <div>
        <h3 class="text-sm font-semibold text-gray-900 mb-1">Forgot Your Password?</h3>
        <p class="text-sm text-gray-500 mb-3">We'll send a password reset link to <strong class="text-gray-700"><%= profile.email %></strong></p>
        <form method="POST" action="/profile/send-reset-email">
          <%- include('partials/csrf') %>
          <button type="submit" class="w-full sm:w-auto px-6 py-2.5 bg-white hover:bg-gray-50 text-gray-700 text-sm font-medium rounded-lg border border-gray-300 transition-colors">
            <svg class="w-4 h-4 inline -mt-0.5 mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>
            Send Reset Link to Email
          </button>
        </form>
      </div>
      <% } else if (!profile.email) { %>
      <hr class="border-gray-200">
      <div class="bg-amber-50 border border-amber-200 rounded-lg p-4">
        <p class="text-sm text-amber-800">
          <svg class="w-4 h-4 inline -mt-0.5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z"/></svg>
          Add an email address above to enable password reset via email.
        </p>
      </div>
      <% } %>

    </div>
  </div>

  <!-- Appearance card (gradient + accent picker) removed in the Atomis
       rebrand — the palette is now locked to the Atomis brand kit. The
       legacy localStorage keys (ts-theme-gradient / ts-theme-accent /
       ts-theme-accent-custom) are intentionally NOT cleaned up; they sit
       unread and harmless until the browser eventually evicts them. -->


  <!-- Push Notifications Card -->
  <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-hidden">
    <div class="px-5 py-4 border-b border-gray-200 bg-gray-50">
      <h2 class="text-base font-semibold text-gray-900">Push Notifications</h2>
    </div>
    <div class="p-5 space-y-4">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-sm font-medium text-gray-900">Mobile / Desktop Notifications</p>
          <p class="text-xs text-gray-500 mt-0.5">Receive instant alerts on your device for tasks, deadlines, and updates</p>
        </div>
        <span id="push-status" class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-500">Checking...</span>
      </div>

      <div id="push-not-supported" class="hidden bg-amber-50 border border-amber-200 rounded-lg p-3">
        <p class="text-sm text-amber-800">
          <svg class="w-4 h-4 inline -mt-0.5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z"/></svg>
          Your browser does not support push notifications. Try Chrome, Edge, or Firefox on a desktop or Android device.
        </p>
      </div>

      <div id="push-denied" class="hidden bg-red-50 border border-red-200 rounded-lg p-3">
        <p class="text-sm text-red-800">
          <svg class="w-4 h-4 inline -mt-0.5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 18.364A9 9 0 005.636 5.636m12.728 12.728A9 9 0 015.636 5.636m12.728 12.728L5.636 5.636"/></svg>
          Notifications are blocked. Open your browser settings and allow notifications for this site, then refresh.
        </p>
      </div>

      <div class="flex flex-col sm:flex-row gap-3">
        <button onclick="sendTestPush()" id="push-test-btn" class="px-4 py-2.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors disabled:opacity-50 disabled:cursor-not-allowed" disabled>
          <svg class="w-4 h-4 inline -mt-0.5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/></svg>
          Send Test Notification
        </button>
      </div>

      <p class="text-xs text-gray-400">Push notifications work on Chrome, Edge, Firefox (desktop & Android). iOS Safari has limited support.</p>
    </div>
  </div>

  <script>
  (function() {
    var statusEl = document.getElementById('push-status');
    var testBtn = document.getElementById('push-test-btn');

    if (!('serviceWorker' in navigator) || !('PushManager' in window)) {
      statusEl.textContent = 'Not Supported';
      statusEl.className = 'inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-amber-50 text-amber-700 ring-1 ring-amber-600/20';
      document.getElementById('push-not-supported').classList.remove('hidden');
      return;
    }

    if (Notification.permission === 'denied') {
      statusEl.textContent = 'Blocked';
      statusEl.className = 'inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-red-50 text-red-700 ring-1 ring-red-600/20';
      document.getElementById('push-denied').classList.remove('hidden');
      return;
    }

    navigator.serviceWorker.ready.then(function(reg) {
      reg.pushManager.getSubscription().then(function(sub) {
        if (sub) {
          statusEl.textContent = 'Enabled';
          statusEl.className = 'inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20';
          testBtn.disabled = false;
        } else {
          statusEl.textContent = 'Not Enabled';
          statusEl.className = 'inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-600';
        }
      });
    });
  })();
  </script>

</div>
