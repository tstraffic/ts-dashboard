<% locals.currentPage = 'jobs' %>

<style>
  /* CSS-only tab navigation */
  .tab-content { display: none; }
  .tab-content:target { display: block; }
  .tab-content:first-of-type:not(:target) { display: block; }
  .tab-content:first-of-type:not(:target) ~ .tab-content:target ~ .tab-content:first-of-type { display: none; }
  /* When any tab is targeted, hide the default first tab */
  .tab-group:has(.tab-content:target) > .tab-content:first-of-type:not(:target) { display: none; }
</style>

<!-- Page Header -->
<div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 mb-6">
  <div class="flex items-center gap-3 flex-wrap">
    <h1 class="text-2xl font-bold text-gray-900"><%= job.job_number %></h1>
    <span class="text-lg text-gray-500">&mdash;</span>
    <span class="text-lg text-gray-600"><%= job.client %></span>
    <!-- Status Badge -->
    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
      <% if (job.status === 'active') { %>bg-blue-50 text-blue-700 ring-1 ring-blue-600/20
      <% } else if (job.status === 'on_hold') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
      <% } else if (job.status === 'completed') { %>bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20
      <% } else if (job.status === 'won') { %>bg-teal-50 text-teal-700 ring-1 ring-teal-600/20
      <% } else if (job.status === 'tender') { %>bg-purple-50 text-purple-700 ring-1 ring-purple-600/20
      <% } else if (job.status === 'prestart') { %>bg-sky-50 text-sky-700 ring-1 ring-sky-600/20
      <% } else if (job.status === 'closed') { %>bg-gray-100/60 text-gray-600
      <% } else { %>bg-white text-gray-600<% } %>">
      <%= job.status ? job.status.replace('_', ' ') : '-' %>
    </span>
    <!-- Health Dot -->
    <% if (job.health) { %>
      <span class="inline-block w-3 h-3 rounded-full
        <% if (job.health === 'green') { %>bg-emerald-500
        <% } else if (job.health === 'amber') { %>bg-brand-600
        <% } else if (job.health === 'red') { %>bg-red-500
        <% } else { %>bg-gray-100<% } %>" title="Health: <%= job.health %>"></span>
    <% } %>
  </div>
  <div class="flex items-center gap-2">
    <a href="/jobs/<%= job.id %>/edit" class="px-4 py-2 bg-gray-100 hover:bg-gray-200 text-gray-600 text-sm font-medium rounded-lg transition-colors">Edit</a>
    <a href="/documents/job/<%= job.id %>" class="px-4 py-2 bg-gray-100 hover:bg-gray-200 text-gray-600 text-sm font-medium rounded-lg transition-colors">Documents</a>
  </div>
</div>

<!-- Info Grid -->
<div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 mb-6">
  <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-x-6 gap-y-4">
    <% /* Always show these core fields */ %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Client</p>
      <p class="text-sm text-gray-900 mt-1"><%= job.client %></p>
    </div>
    <% if (job.project_name) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Project Name</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.project_name %></p>
    </div>
    <% } %>
    <% if (job.client_project_number) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Client Project Number</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.client_project_number %></p>
    </div>
    <% } %>
    <% if (job.principal_contractor) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Principal Contractor</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.principal_contractor %></p>
    </div>
    <% } %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">State</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.state || 'NSW' %></p>
    </div>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Site Address</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.site_address || 'TBC' %></p>
    </div>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Suburb</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.suburb || 'TBC' %></p>
    </div>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Stage</p>
      <p class="text-sm text-gray-600 mt-1 capitalize"><%= job.stage ? job.stage.replace(/_/g, ' ') : 'Not set' %></p>
    </div>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">% Complete</p>
      <div class="flex items-center gap-2 mt-1">
        <div class="flex-1 max-w-[120px] bg-gray-200 rounded-full h-2">
          <div class="h-2 rounded-full <%= (job.percent_complete || 0) >= 100 ? 'bg-emerald-500' : 'bg-blue-500' %>" style="width: <%= Math.min(job.percent_complete || 0, 100) %>%"></div>
        </div>
        <span class="text-sm text-gray-900"><%= job.percent_complete || 0 %>%</span>
      </div>
    </div>
    <% if (job.contract_value) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Contract Value</p>
      <p class="text-sm text-gray-900 mt-1">$<%= Number(job.contract_value).toLocaleString('en-AU', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></p>
    </div>
    <% } %>
    <% if (job.estimated_hours) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Estimated Hours</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.estimated_hours %></p>
    </div>
    <% } %>
    <% if (job.crew_size) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Crew Size</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.crew_size %></p>
    </div>
    <% } %>
    <% if (job.vehicles) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Vehicles</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.vehicles %></p>
    </div>
    <% } %>
    <div class="col-span-2 lg:col-span-4">
      <p class="text-xs text-gray-400 uppercase tracking-wide mb-1.5">Approvals Required</p>
      <div class="flex flex-wrap gap-2">
        <% const approvals = [
          { key: 'rol_required', label: 'ROL' },
          { key: 'tmp_required', label: 'TMP' },
          { key: 'tgs_required', label: 'TGS' },
          { key: 'spa_required', label: 'SPA' },
          { key: 'council_approval', label: 'Council' },
          { key: 'bus_approval', label: 'Bus' },
        ]; %>
        <% approvals.forEach(function(a) { %>
          <% if (job[a.key]) { %>
            <span class="inline-flex items-center px-2.5 py-1 rounded-lg text-xs font-semibold bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20"><%= a.label %></span>
          <% } %>
        <% }) %>
        <% if (!approvals.some(function(a) { return job[a.key]; })) { %>
          <span class="text-sm text-gray-400">None</span>
        <% } %>
      </div>
    </div>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Start Date</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.start_date ? formatDate(job.start_date) : 'TBC' %></p>
    </div>
    <% if (job.end_date) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">End Date</p>
      <p class="text-sm text-gray-600 mt-1"><%= formatDate(job.end_date) %></p>
    </div>
    <% } %>
    <% if (job.pm_name) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Project Manager</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.pm_name %></p>
    </div>
    <% } %>
    <% if (job.ops_name) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Ops Supervisor</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.ops_name %></p>
    </div>
    <% } %>
    <% if (job.traffic_supervisor_name) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Traffic Supervisor</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.traffic_supervisor_name %></p>
    </div>
    <% } %>
    <% if (job.planning_name) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Planning Owner</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.planning_name %></p>
    </div>
    <% } %>
    <% if (job.marketing_name) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Marketing Owner</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.marketing_name %></p>
    </div>
    <% } %>
    <% if (canViewAccounts && job.accounts_name) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">Accounts Owner</p>
      <p class="text-sm text-gray-600 mt-1"><%= job.accounts_name %></p>
    </div>
    <% } %>
    <% if (job.sharepoint_url) { %>
    <div>
      <p class="text-xs text-gray-400 uppercase tracking-wide">SharePoint</p>
      <a href="<%= job.sharepoint_url %>" target="_blank" class="inline-flex items-center gap-1 mt-1 px-3 py-1 bg-blue-50 text-blue-700 text-sm font-medium rounded-lg hover:bg-blue-100 transition-colors">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/></svg>
        Open SharePoint
      </a>
    </div>
    <% } %>
  </div>
</div>

<!-- Tab Navigation -->
<div class="border-b border-gray-200 mb-6">
  <nav class="flex gap-0 -mb-px overflow-x-auto">
    <a href="#overview" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Overview</a>
    <a href="#diary" data-tab="diary" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Site Diary</a>
    <a href="#tasks" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Tasks</a>
    <a href="#updates" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Updates</a>
    <a href="#compliance" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Compliance</a>
    <a href="#plans" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Traffic Plans</a>
    <a href="#incidents" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Incidents</a>
    <a href="#contacts" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Contacts</a>
    <a href="#timesheets" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Timesheets</a>
    <a href="#equipment" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Equipment</a>
    <a href="#defects" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Defects</a>
    <a href="#chat" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Chat</a>
    <% if (canViewAccounts) { %>
    <a href="#budget" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Budget</a>
    <a href="#accounts" class="px-4 py-3 text-sm font-medium border-b-2 transition-colors
      text-gray-500 hover:text-gray-900 border-transparent hover:border-gray-300
      focus:text-brand-700 focus:border-brand-500" onclick="activateTab(this)">Accounts</a>
    <% } %>
  </nav>
</div>

<!-- Tab Content -->
<div class="tab-group">

  <!-- Overview Tab -->
  <div id="overview" class="tab-content">
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6">
      <h2 class="text-lg font-semibold text-gray-900 mb-4">Job Summary</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide mb-2">Details</h3>
          <dl class="space-y-3">
            <div>
              <dt class="text-xs text-gray-400">Job Number</dt>
              <dd class="text-sm text-gray-900"><%= job.job_number %></dd>
            </div>
            <div>
              <dt class="text-xs text-gray-400">Client</dt>
              <dd class="text-sm text-gray-900"><%= job.client %></dd>
            </div>
            <div>
              <dt class="text-xs text-gray-400">Site Address</dt>
              <dd class="text-sm text-gray-600"><%= job.site_address || 'Not specified' %></dd>
            </div>
            <div>
              <dt class="text-xs text-gray-400">Suburb</dt>
              <dd class="text-sm text-gray-600"><%= job.suburb || 'Not specified' %></dd>
            </div>
          </dl>
        </div>
        <div>
          <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide mb-2">Progress</h3>
          <dl class="space-y-3">
            <div>
              <dt class="text-xs text-gray-400">Status</dt>
              <dd class="text-sm">
                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
                  <% if (job.status === 'active') { %>bg-blue-50 text-blue-700 ring-1 ring-blue-600/20
                  <% } else if (job.status === 'on_hold') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
                  <% } else if (job.status === 'completed') { %>bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20
                  <% } else if (job.status === 'won') { %>bg-teal-50 text-teal-700 ring-1 ring-teal-600/20
                  <% } else if (job.status === 'tender') { %>bg-purple-50 text-purple-700 ring-1 ring-purple-600/20
                  <% } else if (job.status === 'prestart') { %>bg-sky-50 text-sky-700 ring-1 ring-sky-600/20
                  <% } else if (job.status === 'closed') { %>bg-gray-100/60 text-gray-600
                  <% } else { %>bg-white text-gray-600<% } %>">
                  <%= job.status ? job.status.replace('_', ' ') : '-' %>
                </span>
              </dd>
            </div>
            <div>
              <dt class="text-xs text-gray-400">Stage</dt>
              <dd class="text-sm text-gray-600"><%= job.stage ? job.stage.replace(/_/g, ' ') : '-' %></dd>
            </div>
            <div>
              <dt class="text-xs text-gray-400">Health</dt>
              <dd class="text-sm flex items-center gap-2">
                <span class="inline-block w-3 h-3 rounded-full
                  <% if (job.health === 'green') { %>bg-emerald-500
                  <% } else if (job.health === 'amber') { %>bg-brand-600
                  <% } else if (job.health === 'red') { %>bg-red-500
                  <% } else { %>bg-gray-100<% } %>"></span>
                <span class="text-gray-600 capitalize"><%= job.health || '-' %></span>
              </dd>
            </div>
            <div>
              <dt class="text-xs text-gray-400">Duration</dt>
              <dd class="text-sm text-gray-600"><%= job.start_date ? formatDate(job.start_date) : '?' %> &rarr; <%= job.end_date ? formatDate(job.end_date) : '?' %></dd>
            </div>
          </dl>
        </div>
      </div>
      <% if (job.notes) { %>
      <div class="mt-6 pt-6 border-t border-gray-200">
        <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide mb-2">Notes</h3>
        <p class="text-sm text-gray-600 whitespace-pre-wrap"><%= job.notes %></p>
      </div>
      <% } %>

      <!-- Recent Site Diary -->
      <% const recentDiary = (typeof diaryEntries !== 'undefined' ? diaryEntries : []).slice(0, 5); %>
      <% if (recentDiary.length > 0) { %>
      <div class="mt-6 pt-6 border-t border-gray-200">
        <div class="flex items-center justify-between mb-3">
          <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide flex items-center gap-2">
            <svg class="w-4 h-4 text-brand-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
            Recent Site Diary
          </h3>
          <button type="button" onclick="document.querySelector('[data-tab=diary]').click()" class="text-xs text-brand-600 hover:text-brand-700 font-medium">View all &rarr;</button>
        </div>
        <div class="space-y-2">
          <% recentDiary.forEach(function(d) { %>
          <div class="flex items-start gap-3 text-sm">
            <span class="text-xs text-gray-400 whitespace-nowrap mt-0.5 font-mono"><%= d.entry_date ? formatDate(d.entry_date) : '' %></span>
            <div class="min-w-0">
              <span class="font-medium text-gray-900"><%= d.task || 'Entry' %></span>
              <% if (d.representative_name || d.representative) { %><span class="text-gray-400"> — <%= d.representative_name || d.representative %></span><% } %>
              <% if (d.outcomes) { %>
                <p class="text-gray-500 text-xs mt-0.5 truncate"><%= d.outcomes %></p>
              <% } %>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
      <% } %>

      <!-- SharePoint Documents Link -->
      <% if (canAccess(user, 'documents')) { %>
      <div class="mt-6 pt-6 border-t border-gray-200">
        <div class="flex items-center justify-between mb-2">
          <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide flex items-center gap-2">
            <svg class="w-4 h-4 text-teal-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4"/></svg>
            SharePoint Documents
          </h3>
        </div>
        <% if (job.sharepoint_url) { %>
          <a href="<%= job.sharepoint_url %>" target="_blank" rel="noopener noreferrer"
            class="inline-flex items-center gap-2 px-4 py-2 bg-teal-50 border border-teal-200 rounded-lg text-teal-700 text-sm hover:bg-teal-100 transition-colors">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/></svg>
            Open in SharePoint
          </a>
          <p class="mt-1 text-xs text-gray-400 break-all"><%= job.sharepoint_url %></p>
        <% } else { %>
          <p class="text-sm text-gray-400">No SharePoint folder linked. Set the SharePoint URL when editing this job.</p>
        <% } %>
      </div>
      <% } %>
    </div>
  </div>

  <!-- Tasks Tab -->
  <div id="tasks" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Tasks</h2>
      <a href="/tasks/new?job_id=<%= job.id %>" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">+ New Task</a>
    </div>
    <% if (tasks.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No tasks for this job yet.</p>
        <a href="/tasks/new?job_id=<%= job.id %>" class="text-brand-600 text-sm hover:underline mt-2 inline-block">Create the first task</a>
      </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Title</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Division</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Owner</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Due Date</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Status</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Priority</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% tasks.forEach(task => { %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3 text-gray-600 font-medium"><%= task.title %></td>
            <td class="px-4 py-3 text-gray-500"><%= task.division || '-' %></td>
            <td class="px-4 py-3 text-gray-600"><%= task.owner_name || '-' %></td>
            <td class="px-4 py-3 text-gray-500 text-xs"><%= task.due_date ? formatDate(task.due_date) : '-' %></td>
            <td class="px-4 py-3">
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
                <% if (task.status === 'completed') { %>bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20
                <% } else if (task.status === 'in_progress') { %>bg-blue-50 text-blue-700 ring-1 ring-blue-600/20
                <% } else if (task.status === 'overdue') { %>bg-red-50 text-red-700 ring-1 ring-red-600/20
                <% } else if (task.status === 'blocked') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
                <% } else { %>bg-white text-gray-600<% } %>">
                <%= task.status ? task.status.replace('_', ' ') : 'pending' %>
              </span>
            </td>
            <td class="px-4 py-3">
              <span class="text-xs font-medium
                <% if (task.priority === 'high') { %>text-red-600
                <% } else if (task.priority === 'medium') { %>text-brand-600
                <% } else { %>text-gray-500<% } %>">
                <%= task.priority || 'low' %>
              </span>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>

  <!-- Updates Tab -->
  <div id="updates" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Project Updates</h2>
      <a href="/updates/new?job_id=<%= job.id %>" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">+ Submit Update</a>
    </div>
    <% if (updates.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No updates submitted for this job yet.</p>
        <a href="/updates/new?job_id=<%= job.id %>" class="text-brand-600 text-sm hover:underline mt-2 inline-block">Submit the first update</a>
      </div>
    <% } else { %>
    <div class="space-y-3">
      <% updates.forEach(update => { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-5">
        <div class="flex items-center justify-between mb-2">
          <span class="text-xs font-medium text-gray-400 uppercase tracking-wide">Week ending <%= update.week_ending %></span>
          <span class="text-xs text-gray-400">by <%= update.submitted_by_name || 'Unknown' %></span>
        </div>
        <p class="text-sm text-gray-600"><%= update.summary %></p>
      </div>
      <% }) %>
    </div>
    <% } %>
  </div>

  <!-- Compliance Tab -->
  <div id="compliance" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Compliance Items</h2>
      <div class="flex items-center gap-2">
        <button type="button" onclick="document.getElementById('link-compliance-form').classList.toggle('hidden')" class="px-3 py-1.5 bg-gray-100 hover:bg-gray-200 text-gray-700 text-sm font-medium rounded-lg transition-colors">Link Existing</button>
        <a href="/compliance/new?job_id=<%= job.id %>&return_to=/projects/<%= job.id %>%23compliance" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">+ New Item</a>
      </div>
    </div>
    <!-- Link existing compliance item to this job -->
    <div id="link-compliance-form" class="hidden bg-white border border-gray-200 rounded-xl shadow-sm p-4 mb-4">
      <form method="POST" action="/jobs/<%= job.id %>/link-compliance">
        <%- include('../partials/csrf') %>
        <div class="flex items-end gap-3">
          <div class="flex-1">
            <label class="block text-xs font-medium text-gray-500 mb-1">Select item from Plans & Approvals register</label>
            <select name="compliance_id" required id="link-compliance-select" class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500">
              <option value="">Choose an item...</option>
            </select>
            <p class="text-xs text-gray-400 mt-1">Shows unlinked items or items from other jobs</p>
          </div>
          <button type="submit" class="px-4 py-2 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors whitespace-nowrap">Link to Job</button>
          <button type="button" onclick="document.getElementById('link-compliance-form').classList.add('hidden')" class="px-4 py-2 bg-gray-100 text-gray-600 text-sm font-medium rounded-lg">Cancel</button>
        </div>
      </form>
    </div>
    <% if (complianceItems.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No compliance items for this job yet.</p>
        <a href="/compliance/new?job_id=<%= job.id %>" class="text-brand-600 text-sm hover:underline mt-2 inline-block">Create the first compliance item</a>
      </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Title</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Type</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Status</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Due Date</th>
            <th class="px-4 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wide">Cost</th>
            <th class="px-4 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wide">Charged</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Approver</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Actions</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% let compTotalCost = 0, compTotalCharged = 0; %>
          <% complianceItems.forEach(item => { %>
          <% const itemCost = (item.costs || 0) + (item.council_fee_amount || 0); compTotalCost += itemCost; compTotalCharged += (item.charge_amount || 0); %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3 font-medium">
              <a href="/compliance/<%= item.id %>/edit?return_to=/jobs/<%= job.id %>" class="text-brand-600 hover:text-brand-700 hover:underline"><%= item.title %></a>
              <% if (item.reference_number) { %><span class="text-xs text-gray-400 ml-1">(<%= item.reference_number %>)</span><% } %>
            </td>
            <td class="px-4 py-3 text-gray-500"><%= item.item_type ? item.item_type.replace(/_/g, ' ') : '-' %></td>
            <td class="px-4 py-3">
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
                <% if (item.status === 'approved') { %>bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20
                <% } else if (item.status === 'submitted') { %>bg-blue-50 text-blue-700 ring-1 ring-blue-600/20
                <% } else if (item.status === 'overdue') { %>bg-red-50 text-red-700 ring-1 ring-red-600/20
                <% } else if (item.status === 'expiring') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
                <% } else { %>bg-white text-gray-600<% } %>">
                <%= item.status ? item.status.replace('_', ' ') : 'pending' %>
              </span>
            </td>
            <td class="px-4 py-3 text-gray-500 text-xs"><%= item.due_date ? formatDate(item.due_date) : '-' %></td>
            <td class="px-4 py-3 text-right text-gray-900 font-medium"><%= itemCost > 0 ? '$' + itemCost.toLocaleString() : '-' %></td>
            <td class="px-4 py-3 text-right">
              <% if (item.charge_amount > 0) { %>
                <span class="text-gray-900 font-medium">$<%= item.charge_amount.toLocaleString() %></span>
                <% if (item.invoiced) { %><span class="ml-1 inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-medium bg-emerald-50 text-emerald-700">Invoiced</span><% } %>
              <% } else { %>-<% } %>
            </td>
            <td class="px-4 py-3 text-gray-600"><%= item.approver_name || '-' %></td>
            <td class="px-4 py-3 whitespace-nowrap">
              <a href="/compliance/<%= item.id %>/edit" class="text-brand-600 text-xs font-medium mr-2">Edit</a>
              <form method="POST" action="/jobs/<%= job.id %>/unlink-compliance" class="inline" onsubmit="return confirm('Unlink this item?')">
                <%- include('../partials/csrf') %>
                <input type="hidden" name="compliance_id" value="<%= item.id %>">
                <input type="hidden" name="redirect_hash" value="compliance">
                <button type="submit" class="text-red-500 hover:text-red-700 text-xs font-medium">Unlink</button>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
        <% if (compTotalCost > 0 || compTotalCharged > 0) { %>
        <tfoot>
          <tr class="border-t-2 border-gray-300 bg-gray-50">
            <td class="px-4 py-3 text-sm font-semibold text-gray-900" colspan="4">Total</td>
            <td class="px-4 py-3 text-right text-sm font-bold text-gray-900">$<%= compTotalCost.toLocaleString() %></td>
            <td class="px-4 py-3 text-right text-sm font-bold text-gray-900"><%= compTotalCharged > 0 ? '$' + compTotalCharged.toLocaleString() : '-' %></td>
            <td></td>
          </tr>
        </tfoot>
        <% } %>
      </table>
    </div>
    <% } %>
  </div>

  <!-- Traffic Plans Tab -->
  <div id="plans" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Traffic Plans</h2>
      <div class="flex items-center gap-2">
        <button type="button" onclick="document.getElementById('link-plan-form').classList.toggle('hidden')" class="px-3 py-1.5 bg-gray-100 hover:bg-gray-200 text-gray-700 text-sm font-medium rounded-lg transition-colors">Link Existing</button>
        <a href="/compliance/new?job_id=<%= job.id %>&return_to=/projects/<%= job.id %>%23plans" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">+ New Plan</a>
      </div>
    </div>
    <!-- Link existing plan from register -->
    <div id="link-plan-form" class="hidden bg-white border border-gray-200 rounded-xl shadow-sm p-4 mb-4">
      <form method="POST" action="/jobs/<%= job.id %>/link-compliance">
        <%- include('../partials/csrf') %>
        <input type="hidden" name="redirect_hash" value="plans">
        <div class="flex items-end gap-3">
          <div class="flex-1">
            <label class="block text-xs font-medium text-gray-500 mb-1">Select plan from register</label>
            <select name="compliance_id" required id="link-plan-select" class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500">
              <option value="">Choose a plan...</option>
            </select>
          </div>
          <button type="submit" class="px-4 py-2 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors whitespace-nowrap">Link to Job</button>
          <button type="button" onclick="document.getElementById('link-plan-form').classList.add('hidden')" class="px-4 py-2 bg-gray-100 text-gray-600 text-sm font-medium rounded-lg">Cancel</button>
        </div>
      </form>
    </div>
    <%
      // Combine traffic_plans + compliance items with plan-related types for this tab
      const planTypes = ['traffic_guidance','tmp_approval','spa','rol'];
      const compliancePlans = (typeof complianceItems !== 'undefined' ? complianceItems : []).filter(function(c) {
        const types = (c.item_types || c.item_type || '').split(',');
        return types.some(function(t) { return planTypes.includes(t.trim()); });
      });
      const allPlans = compliancePlans;
      // Also include traffic_plans entries
      const tpEntries = typeof trafficPlans !== 'undefined' ? trafficPlans : [];
    %>
    <% if (allPlans.length === 0 && tpEntries.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No traffic plans for this job yet.</p>
        <a href="/compliance/new?job_id=<%= job.id %>&return_to=/projects/<%= job.id %>%23plans" class="text-brand-600 text-sm hover:underline mt-2 inline-block">Create the first plan</a>
      </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Title / Ref</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Types</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Designer</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Status</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Due</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Expiry</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Actions</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% allPlans.forEach(function(item) {
            const types = (item.item_types || item.item_type || '').split(',').filter(Boolean);
            const typeLabels = { traffic_guidance: 'TGS', tmp_approval: 'CTMP', spa: 'SPA', rol: 'ROL', council_permit: 'Council', bus_approval: 'Bus' };
            const statusColors = { not_started: 'bg-gray-100 text-gray-600', started: 'bg-purple-50 text-purple-700', submitted: 'bg-blue-50 text-blue-700', approved: 'bg-emerald-50 text-emerald-700', rejected: 'bg-red-50 text-red-700', expired: 'bg-amber-50 text-amber-700' };
          %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3">
              <a href="/compliance/<%= item.id %>/edit" class="text-brand-600 hover:text-brand-700 font-medium"><%= item.title %></a>
              <% if (item.reference_number) { %><div class="text-xs text-gray-400"><%= item.reference_number %></div><% } %>
            </td>
            <td class="px-4 py-3">
              <div class="flex flex-wrap gap-1">
                <% types.forEach(function(t) { %>
                  <span class="inline-flex px-1.5 py-0.5 text-[10px] font-bold rounded-full bg-blue-50 text-blue-700"><%= typeLabels[t] || t %></span>
                <% }) %>
              </div>
            </td>
            <td class="px-4 py-3 text-gray-600 text-xs"><%= item.designer || item.approver_name || '-' %></td>
            <td class="px-4 py-3">
              <span class="inline-flex px-2 py-0.5 text-[10px] font-bold rounded-full <%= statusColors[item.status] || 'bg-gray-100 text-gray-600' %>"><%= (item.status || 'not_started').replace(/_/g, ' ') %></span>
            </td>
            <td class="px-4 py-3 text-gray-500 text-xs"><%= item.due_date ? formatDateShort(item.due_date) : '-' %></td>
            <td class="px-4 py-3 text-gray-500 text-xs"><%= item.expiry_date ? formatDateShort(item.expiry_date) : '-' %></td>
            <td class="px-4 py-3 whitespace-nowrap">
              <a href="/compliance/<%= item.id %>/edit" class="text-brand-600 hover:text-brand-700 text-xs font-medium mr-2">Edit</a>
              <form method="POST" action="/jobs/<%= job.id %>/unlink-compliance" class="inline" onsubmit="return confirm('Unlink this item from the job?')">
                <%- include('../partials/csrf') %>
                <input type="hidden" name="compliance_id" value="<%= item.id %>">
                <input type="hidden" name="redirect_hash" value="plans">
                <button type="submit" class="text-red-500 hover:text-red-700 text-xs font-medium">Unlink</button>
              </form>
            </td>
          </tr>
          <% }) %>
          <% tpEntries.forEach(function(tp) { %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3 text-brand-600 font-medium"><%= tp.plan_number %></td>
            <td class="px-4 py-3">
              <span class="inline-flex px-1.5 py-0.5 text-[10px] font-bold rounded-full bg-blue-50 text-blue-700"><%= tp.plan_type %></span>
            </td>
            <td class="px-4 py-3 text-gray-600 text-xs"><%= tp.designer || '-' %></td>
            <td class="px-4 py-3">
              <span class="inline-flex px-2 py-0.5 text-[10px] font-bold rounded-full
                <% if (tp.status === 'approved') { %>bg-emerald-50 text-emerald-700
                <% } else if (tp.status === 'submitted') { %>bg-blue-50 text-blue-700
                <% } else { %>bg-gray-100 text-gray-600<% } %>"><%= tp.status ? tp.status.replace(/_/g, ' ') : 'draft' %></span>
            </td>
            <td class="px-4 py-3 text-gray-500 text-xs"><%= tp.submitted_date ? formatDate(tp.submitted_date) : '-' %></td>
            <td class="px-4 py-3 text-gray-500 text-xs"><%= tp.expiry_date ? formatDate(tp.expiry_date) : '-' %></td>
            <td class="px-4 py-3">
              <a href="/compliance/<%= tp.id %>/edit" class="text-brand-600 hover:text-brand-700 text-xs font-medium">Edit</a>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>

  <!-- Incidents Tab -->
  <div id="incidents" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Safety & Incidents</h2>
      <a href="/incidents/new?job_id=<%= job.id %>" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">+ Report Incident</a>
    </div>
    <% if (incidents.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No incidents recorded for this job.</p>
      </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Date</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Type</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Severity</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Description</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Status</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Reported By</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% incidents.forEach(inc => { %>
          <tr class="hover:bg-gray-50 cursor-pointer" onclick="window.location='/incidents/<%= inc.id %>'">
            <td class="px-4 py-3 text-gray-600 text-xs"><%= inc.incident_date ? formatDate(inc.incident_date) : '-' %></td>
            <td class="px-4 py-3 text-gray-600"><%= inc.incident_type ? inc.incident_type.replace(/_/g, ' ') : '-' %></td>
            <td class="px-4 py-3">
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
                <% if (inc.severity === 'critical') { %>bg-red-50 text-red-700 ring-1 ring-red-600/20
                <% } else if (inc.severity === 'high') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
                <% } else if (inc.severity === 'medium') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
                <% } else { %>bg-white text-gray-600<% } %>"><%= inc.severity %></span>
            </td>
            <td class="px-4 py-3 text-gray-600 max-w-xs truncate"><%= inc.description ? inc.description.substring(0, 80) : '-' %></td>
            <td class="px-4 py-3">
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
                <% if (inc.investigation_status === 'closed' || inc.investigation_status === 'resolved') { %>bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20
                <% } else if (inc.investigation_status === 'investigating') { %>bg-blue-50 text-blue-700 ring-1 ring-blue-600/20
                <% } else { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20<% } %>"><%= inc.investigation_status ? inc.investigation_status.replace(/_/g, ' ') : 'reported' %></span>
            </td>
            <td class="px-4 py-3 text-gray-500 text-xs"><%= inc.reported_by_name || '-' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>

  <!-- Contacts Tab -->
  <div id="contacts" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Client Contacts</h2>
      <a href="/contacts/new?job_id=<%= job.id %>" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">+ Add Contact</a>
    </div>
    <% if (contacts.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No contacts for this job yet.</p>
      </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Name</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Type</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Organisation</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Phone</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Email</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Primary</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% contacts.forEach(c => { %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3 text-gray-600 font-medium"><%= c.full_name %></td>
            <td class="px-4 py-3 text-gray-500"><%= c.contact_type ? c.contact_type.replace(/_/g, ' ') : '-' %></td>
            <td class="px-4 py-3 text-gray-500"><%= c.company || '-' %></td>
            <td class="px-4 py-3 text-gray-500"><%= c.phone || '-' %></td>
            <td class="px-4 py-3 text-gray-500"><%= c.email || '-' %></td>
            <td class="px-4 py-3"><%= c.is_primary ? '✓' : '' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>

  <!-- Timesheets Tab -->
  <div id="timesheets" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Timesheets</h2>
      <a href="/timesheets/new?job_id=<%= job.id %>" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">+ Add Timesheet</a>
    </div>
    <% if (timesheets.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No timesheets recorded for this job yet.</p>
      </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Date</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Crew Member</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Start</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">End</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Hours</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Shift</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Status</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% timesheets.forEach(ts => { %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3 text-gray-600 text-xs"><%= ts.work_date ? formatDate(ts.work_date) : '-' %></td>
            <td class="px-4 py-3 text-gray-600"><%= ts.crew_name || '-' %></td>
            <td class="px-4 py-3 text-gray-500"><%= ts.start_time || '-' %></td>
            <td class="px-4 py-3 text-gray-500"><%= ts.end_time || '-' %></td>
            <td class="px-4 py-3 text-gray-900 font-medium"><%= ts.total_hours || '-' %></td>
            <td class="px-4 py-3 text-gray-500"><%= ts.shift_type || 'day' %></td>
            <td class="px-4 py-3">
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
                <% if (ts.approved) { %>bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20
                <% } else { %>bg-white text-gray-600<% } %>"><%= ts.approved ? 'Approved' : 'Pending' %></span>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>

  <!-- Equipment Tab -->
  <div id="equipment" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Equipment Deployed</h2>
      <a href="/equipment" class="px-3 py-1.5 bg-gray-100 hover:bg-gray-200 text-gray-600 text-sm font-medium rounded-lg transition-colors">View All Equipment</a>
    </div>
    <% if (equipmentAssignments.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No equipment assigned to this job.</p>
      </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Asset #</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Equipment</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Category</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Condition</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Assigned</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Returned</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% equipmentAssignments.forEach(ea => { %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3 font-medium"><a href="/equipment/<%= ea.equipment_id %>" class="text-brand-600 hover:text-brand-700 hover:underline"><%= ea.asset_number || '-' %></a></td>
            <td class="px-4 py-3"><a href="/equipment/<%= ea.equipment_id %>" class="text-gray-700 hover:text-brand-600 hover:underline"><%= ea.equipment_name || '-' %></a></td>
            <td class="px-4 py-3 text-gray-500"><%= ea.category ? ea.category.replace(/_/g, ' ') : '-' %></td>
            <td class="px-4 py-3">
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
                <% if (ea.equipment_condition === 'new' || ea.equipment_condition === 'good') { %>bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20
                <% } else if (ea.equipment_condition === 'fair') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
                <% } else { %>bg-red-50 text-red-700 ring-1 ring-red-600/20<% } %>"><%= ea.equipment_condition || '-' %></span>
            </td>
            <td class="px-4 py-3 text-gray-500 text-xs"><%= ea.assigned_date ? formatDate(ea.assigned_date) : '-' %></td>
            <td class="px-4 py-3 text-xs"><%= ea.actual_return_date ? '<span class="text-emerald-600">' + formatDate(ea.actual_return_date) + '</span>' : '<span class="text-brand-600">On site</span>' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>

  <!-- Defects Tab -->
  <div id="defects" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Defects & Snags</h2>
      <a href="/defects/new?job_id=<%= job.id %>" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">+ Report Defect</a>
    </div>
    <% if (defects.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No defects recorded for this job.</p>
      </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Ref</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Title</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Severity</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Status</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Assigned To</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Target Close</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% defects.forEach(d => { %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3 text-brand-600 font-medium text-xs"><%= d.defect_number || '-' %></td>
            <td class="px-4 py-3 text-gray-600"><%= d.title %></td>
            <td class="px-4 py-3">
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
                <% if (d.severity === 'critical') { %>bg-red-50 text-red-700 ring-1 ring-red-600/20
                <% } else if (d.severity === 'major') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
                <% } else if (d.severity === 'moderate') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
                <% } else { %>bg-white text-gray-600<% } %>"><%= d.severity %></span>
            </td>
            <td class="px-4 py-3">
              <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium
                <% if (d.status === 'closed') { %>bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20
                <% } else if (d.status === 'rectification') { %>bg-blue-50 text-blue-700 ring-1 ring-blue-600/20
                <% } else if (d.status === 'investigating') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
                <% } else { %>bg-red-50 text-red-700 ring-1 ring-red-600/20<% } %>"><%= d.status ? d.status.replace(/_/g, ' ') : 'open' %></span>
            </td>
            <td class="px-4 py-3 text-gray-500"><%= d.assigned_to_name || '-' %></td>
            <td class="px-4 py-3 text-gray-500 text-xs"><%= d.target_close_date ? formatDate(d.target_close_date) : '-' %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>

  <!-- Site Diary Tab -->
  <div id="diary" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Site Diary</h2>
      <button onclick="document.getElementById('diary-form').classList.toggle('hidden')" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">+ New Entry</button>
    </div>

    <!-- Inline Add Form -->
    <div id="diary-form" class="hidden bg-white border border-gray-200 rounded-xl shadow-sm p-4 mb-4">
      <form method="POST" action="/jobs/<%= job.id %>/diary" enctype="multipart/form-data">
        <%- include('../partials/csrf') %>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3">
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Date *</label>
            <input type="date" name="entry_date" required value="<%= new Date().toISOString().split('T')[0] %>"
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Task</label>
            <input type="text" name="task" placeholder="e.g. Site Walk, TGS received..."
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Representative</label>
            <select name="representative_id" class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
              <option value="">Select staff...</option>
              <% (typeof allUsers !== 'undefined' ? allUsers : []).forEach(function(u) { %>
                <option value="<%= u.id %>"><%= u.full_name %></option>
              <% }) %>
            </select>
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Client Representative</label>
            <input type="text" name="client_representative" placeholder="Client contact..."
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div class="sm:col-span-2">
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Outcomes</label>
            <input type="text" name="outcomes" placeholder="What was achieved..."
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Issues</label>
            <input type="text" name="issues" placeholder="Any issues..."
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Comments</label>
            <input type="text" name="comments" placeholder="Additional notes..."
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Stage</label>
            <input type="text" name="stage" placeholder="e.g. Stage 1..."
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">TGS No.</label>
            <input type="text" name="tgs_number" placeholder="e.g. 1892..."
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">TGS Scope</label>
            <input type="text" name="tgs_scope" placeholder="e.g. Tree Lopping..."
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">TGS Link</label>
            <select name="tgs_plan_id" class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
              <option value="">None</option>
              <% if (typeof tgsPlans !== 'undefined') { tgsPlans.forEach(function(tp) { %>
                <option value="<%= tp.id %>"><%= tp.plan_number %></option>
              <% }) } %>
            </select>
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Compliance/TGS Item</label>
            <select name="compliance_item_id" class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
              <option value="">None</option>
              <% (typeof complianceTgsItems !== 'undefined' ? complianceTgsItems : []).forEach(function(c) { %>
                <option value="<%= c.id %>"><%= c.title %></option>
              <% }) %>
            </select>
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Equipment</label>
            <select name="equipment_assignment_id" class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
              <option value="">None</option>
              <% (typeof equipmentAssignments !== 'undefined' ? equipmentAssignments : []).forEach(function(ea) { %>
                <option value="<%= ea.id %>"><%= ea.equipment_name %> (<%= ea.asset_number %>)</option>
              <% }) %>
            </select>
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">Attachments</label>
            <input type="file" name="attachments" multiple accept=".pdf,.doc,.docx,.xls,.xlsx,.jpg,.jpeg,.png"
              class="w-full text-sm text-gray-500 file:mr-2 file:py-1.5 file:px-3 file:rounded-lg file:border-0 file:text-xs file:font-medium file:bg-brand-50 file:text-brand-700 hover:file:bg-brand-100 cursor-pointer">
          </div>
          <div>
            <label class="block text-[10px] font-bold text-gray-400 uppercase tracking-widest mb-1">SharePoint Link</label>
            <input type="url" name="sharepoint_link" placeholder="https://..."
              class="w-full px-3 py-2 bg-white border border-gray-300 rounded-lg text-gray-900 text-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-brand-500 focus:border-transparent">
          </div>
        </div>
        <div class="flex items-center gap-3 mt-4">
          <button type="submit" class="px-4 py-2 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">Add Entry</button>
          <button type="button" onclick="document.getElementById('diary-form').classList.add('hidden')" class="px-4 py-2 bg-gray-100 hover:bg-gray-200 text-gray-600 text-sm font-medium rounded-lg transition-colors">Cancel</button>
        </div>
      </form>
    </div>

    <% if (typeof diaryEntries === 'undefined' || diaryEntries.length === 0) { %>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
        <p class="text-gray-400 text-sm">No diary entries yet.</p>
        <button onclick="document.getElementById('diary-form').classList.remove('hidden')" class="text-brand-600 text-sm hover:underline mt-2 inline-block">Add the first entry</button>
      </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-3 py-2.5 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Date</th>
            <th class="px-3 py-2.5 text-left text-xs font-medium text-gray-500 uppercase tracking-wide">Task</th>
            <th class="px-3 py-2.5 text-left text-xs font-medium text-gray-500 uppercase tracking-wide hidden md:table-cell">Representative</th>
            <th class="px-3 py-2.5 text-left text-xs font-medium text-gray-500 uppercase tracking-wide hidden md:table-cell">Client Rep</th>
            <th class="px-3 py-2.5 text-left text-xs font-medium text-gray-500 uppercase tracking-wide hidden lg:table-cell">Outcomes</th>
            <th class="px-3 py-2.5 text-left text-xs font-medium text-gray-500 uppercase tracking-wide hidden xl:table-cell">Issues</th>
            <th class="px-3 py-2.5 text-left text-xs font-medium text-gray-500 uppercase tracking-wide hidden xl:table-cell">Comments</th>
            <th class="px-3 py-2.5 text-left text-xs font-medium text-gray-500 uppercase tracking-wide hidden lg:table-cell">Stage</th>
            <th class="px-3 py-2.5 text-left text-xs font-medium text-gray-500 uppercase tracking-wide hidden xl:table-cell">TGS</th>
            <th class="px-3 py-2.5 text-right text-xs font-medium text-gray-500 uppercase tracking-wide">Actions</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-100">
          <% diaryEntries.forEach(function(entry) {
            const entryAttach = (typeof diaryAttachments !== 'undefined' ? diaryAttachments : []).filter(a => a.diary_entry_id === entry.id);
          %>
          <tr class="hover:bg-gray-50">
            <td class="px-3 py-2.5 text-gray-600 text-xs whitespace-nowrap"><%= entry.entry_date ? formatDate(entry.entry_date) : '' %></td>
            <td class="px-3 py-2.5 text-gray-900 font-medium max-w-[200px] truncate">
              <%= entry.task || '-' %>
              <% if (entryAttach.length > 0) { %>
                <span class="inline-flex items-center gap-0.5 ml-1 text-gray-400" title="<%= entryAttach.length %> attachment(s)">
                  <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.172 7l-6.586 6.586a2 2 0 102.828 2.828l6.414-6.586a4 4 0 00-5.656-5.656l-6.415 6.585a6 6 0 108.486 8.486L20.5 13"/></svg>
                  <span class="text-[10px]"><%= entryAttach.length %></span>
                </span>
              <% } %>
            </td>
            <td class="px-3 py-2.5 text-gray-600 text-xs hidden md:table-cell"><%= entry.representative_name || entry.representative || '-' %></td>
            <td class="px-3 py-2.5 text-gray-600 text-xs hidden md:table-cell"><%= entry.client_representative || '-' %></td>
            <td class="px-3 py-2.5 text-gray-600 text-xs hidden lg:table-cell max-w-[200px] truncate"><%= entry.outcomes || '-' %></td>
            <td class="px-3 py-2.5 text-gray-600 text-xs hidden xl:table-cell"><%= entry.issues || '-' %></td>
            <td class="px-3 py-2.5 text-gray-600 text-xs hidden xl:table-cell max-w-[150px] truncate"><%= entry.comments || '-' %></td>
            <td class="px-3 py-2.5 text-gray-600 text-xs hidden lg:table-cell"><%= entry.stage || '-' %></td>
            <td class="px-3 py-2.5 hidden xl:table-cell">
              <% if (entry.compliance_item_id) { %>
                <a href="/compliance/<%= entry.compliance_item_id %>/edit" class="text-teal-600 hover:underline text-xs"><%= entry.compliance_item_title %></a>
              <% } else if (entry.tgs_plan_id) { %>
                <a href="/compliance/<%= entry.tgs_plan_id %>/edit" class="text-brand-600 hover:underline text-xs"><%= entry.tgs_plan_number || entry.tgs_number %></a>
              <% } else if (entry.tgs_number) { %>
                <span class="text-gray-600 text-xs"><%= entry.tgs_number %> <% if (entry.tgs_scope) { %>(<%= entry.tgs_scope %>)<% } %></span>
              <% } else { %>
                <span class="text-gray-300 text-xs">-</span>
              <% } %>
            </td>
            <td class="px-3 py-2.5 text-right whitespace-nowrap">
              <button type="button" onclick="document.getElementById('edit-diary-<%= entry.id %>').classList.toggle('hidden')" class="p-1.5 text-gray-400 hover:text-brand-600 hover:bg-brand-50 rounded-lg transition" title="Edit">
                <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/></svg>
              </button>
              <form method="POST" action="/jobs/<%= job.id %>/diary/<%= entry.id %>/delete" class="inline" onsubmit="return confirm('Delete this entry?')">
                <%- include('../partials/csrf') %>
                <button type="submit" class="p-1.5 text-gray-400 hover:text-red-600 hover:bg-red-50 rounded-lg transition" title="Delete">
                  <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/></svg>
                </button>
              </form>
            </td>
          </tr>
          <!-- Inline Edit Row -->
          <tr id="edit-diary-<%= entry.id %>" class="hidden">
            <td colspan="10" class="px-3 py-3 bg-gray-50">
              <form method="POST" action="/jobs/<%= job.id %>/diary/<%= entry.id %>" enctype="multipart/form-data">
                <%- include('../partials/csrf') %>
                <div class="grid grid-cols-2 lg:grid-cols-4 gap-2 text-xs">
                  <input type="date" name="entry_date" value="<%= entry.entry_date %>" required class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                  <input type="text" name="task" value="<%= entry.task || '' %>" placeholder="Task" class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                  <select name="representative_id" class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                    <option value="">Rep...</option>
                    <% (typeof allUsers !== 'undefined' ? allUsers : []).forEach(function(u) { %>
                      <option value="<%= u.id %>" <%= entry.representative_id == u.id ? 'selected' : '' %>><%= u.full_name %></option>
                    <% }) %>
                  </select>
                  <input type="text" name="client_representative" value="<%= entry.client_representative || '' %>" placeholder="Client rep" class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                  <input type="text" name="outcomes" value="<%= entry.outcomes || '' %>" placeholder="Outcomes" class="col-span-2 px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                  <input type="text" name="issues" value="<%= entry.issues || '' %>" placeholder="Issues" class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                  <input type="text" name="comments" value="<%= entry.comments || '' %>" placeholder="Comments" class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                  <input type="text" name="stage" value="<%= entry.stage || '' %>" placeholder="Stage" class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                  <input type="text" name="tgs_number" value="<%= entry.tgs_number || '' %>" placeholder="TGS No." class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                  <input type="text" name="tgs_scope" value="<%= entry.tgs_scope || '' %>" placeholder="TGS Scope" class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                  <select name="compliance_item_id" class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                    <option value="">Compliance link...</option>
                    <% (typeof complianceTgsItems !== 'undefined' ? complianceTgsItems : []).forEach(function(c) { %>
                      <option value="<%= c.id %>" <%= entry.compliance_item_id == c.id ? 'selected' : '' %>><%= c.title %></option>
                    <% }) %>
                  </select>
                  <select name="equipment_assignment_id" class="px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
                    <option value="">Equipment...</option>
                    <% (typeof equipmentAssignments !== 'undefined' ? equipmentAssignments : []).forEach(function(ea) { %>
                      <option value="<%= ea.id %>" <%= entry.equipment_assignment_id == ea.id ? 'selected' : '' %>><%= ea.equipment_name %></option>
                    <% }) %>
                  </select>
                  <input type="file" name="attachments" multiple accept=".pdf,.doc,.docx,.jpg,.jpeg,.png" class="col-span-2 text-xs text-gray-500 file:mr-2 file:py-1 file:px-2 file:rounded file:border-0 file:text-xs file:bg-brand-50 file:text-brand-700">
                </div>
                <% if (entryAttach.length > 0) { %>
                <div class="flex flex-wrap gap-2 mt-2">
                  <% entryAttach.forEach(function(a) { %>
                    <% if (a.sharepoint_link) { %>
                      <a href="<%= a.sharepoint_link %>" target="_blank" class="inline-flex items-center gap-1 px-2 py-1 bg-blue-50 text-blue-700 text-xs rounded-lg hover:bg-blue-100">
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/></svg>
                        SharePoint
                      </a>
                    <% } else { %>
                      <a href="<%= a.file_path %>" target="_blank" class="inline-flex items-center gap-1 px-2 py-1 bg-gray-100 text-gray-700 text-xs rounded-lg hover:bg-gray-200">
                        <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.172 7l-6.586 6.586a2 2 0 102.828 2.828l6.414-6.586a4 4 0 00-5.656-5.656l-6.415 6.585a6 6 0 108.486 8.486L20.5 13"/></svg>
                        <%= a.original_name %>
                      </a>
                    <% } %>
                  <% }) %>
                </div>
                <% } %>
                <div class="flex gap-2 mt-2">
                  <button type="submit" class="px-3 py-1.5 bg-brand-600 text-white text-xs font-medium rounded-lg hover:bg-brand-500">Save</button>
                  <button type="button" onclick="document.getElementById('edit-diary-<%= entry.id %>').classList.add('hidden')" class="px-3 py-1.5 bg-gray-100 text-gray-600 text-xs font-medium rounded-lg hover:bg-gray-200">Cancel</button>
                </div>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>

  <!-- Chat Tab -->
  <div id="chat" class="tab-content">
    <div class="mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Job Chat</h2>
      <p class="text-sm text-gray-500 mt-1">Operational messages for this job</p>
      <% if (typeof chatMembers !== 'undefined') { %>
      <div class="mt-2">
        <div class="flex items-center gap-2 mb-1">
          <span class="text-xs text-gray-400 uppercase tracking-wide font-medium">Members (<%= chatMembers.length %>)</span>
          <button type="button" onclick="document.getElementById('add-chat-member').classList.toggle('hidden')" class="w-5 h-5 flex items-center justify-center rounded-full bg-brand-600 hover:bg-brand-500 text-white text-xs transition" title="Add member">+</button>
        </div>
        <div class="flex flex-wrap gap-1.5">
          <% chatMembers.forEach(function(m) { %>
            <span class="inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-700"><%= m.full_name %></span>
          <% }) %>
        </div>
        <!-- Add member form -->
        <div id="add-chat-member" class="hidden mt-2">
          <form method="POST" action="/jobs/<%= job.id %>/chat/add-member" class="flex items-center gap-2">
            <%- include('../partials/csrf') %>
            <select name="user_id" required class="flex-1 px-2 py-1.5 bg-white border border-gray-300 rounded-lg text-gray-900 text-xs focus:ring-2 focus:ring-brand-500">
              <option value="">Select user...</option>
              <% const memberIds = chatMembers.map(function(m) { return m.id; }); %>
              <% (typeof allUsers !== 'undefined' ? allUsers : []).forEach(function(u) { %>
                <% if (!memberIds.includes(u.id)) { %>
                  <option value="<%= u.id %>"><%= u.full_name %></option>
                <% } %>
              <% }) %>
            </select>
            <button type="submit" class="px-3 py-1.5 bg-brand-600 text-white text-xs font-medium rounded-lg hover:bg-brand-500 transition">Add</button>
          </form>
        </div>
      </div>
      <% } %>
    </div>
    <% if (typeof chatThreadId !== 'undefined' && chatThreadId) { %>
      <%- include('../partials/chat-panel') %>
    <% } else { %>
      <div class="bg-white border border-gray-200 rounded-xl p-6 text-center">
        <p class="text-gray-400 text-sm">Chat thread not available.</p>
      </div>
    <% } %>
  </div>

  <!-- Budget Tab (conditional) -->
  <% if (canViewAccounts) { %>
  <div id="budget" class="tab-content">
    <div class="flex items-center justify-between mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Budget & Costs</h2>
      <a href="/budgets/job/<%= job.id %>" class="px-3 py-1.5 bg-brand-600 hover:bg-brand-500 text-white text-sm font-medium rounded-lg transition-colors">Edit Budget</a>
    </div>
    <% if (budget) { %>
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-4">
        <p class="text-xs text-gray-500 uppercase">Contract Value</p>
        <p class="text-2xl font-bold text-gray-900 mt-1">$<%= (budget.contract_value || 0).toLocaleString() %></p>
      </div>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-4">
        <p class="text-xs text-gray-500 uppercase">Total Budget</p>
        <p class="text-2xl font-bold text-blue-600 mt-1">$<%= ((budget.labour || 0) + (budget.materials || 0) + (budget.subcontractors || 0) + (budget.equipment || 0) + (budget.other || 0)).toLocaleString() %></p>
      </div>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-4">
        <p class="text-xs text-gray-500 uppercase">Total Spend</p>
        <p class="text-2xl font-bold text-brand-700 mt-1">$<%= totalSpend.toLocaleString() %></p>
      </div>
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-4">
        <p class="text-xs text-gray-500 uppercase">Margin</p>
        <% const margin = (budget.contract_value || 0) - totalSpend; %>
        <p class="text-2xl font-bold <%= margin >= 0 ? 'text-emerald-600' : 'text-red-600' %> mt-1">$<%= margin.toLocaleString() %></p>
      </div>
    </div>
    <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center mb-6">
      <p class="text-gray-400 text-sm">No budget set for this job yet.</p>
      <a href="/budgets/job/<%= job.id %>" class="text-brand-600 text-sm hover:underline mt-2 inline-block">Set up budget</a>
    </div>
    <% } %>
    <!-- Cost Breakdown by Source -->
    <% const allSourcesTotal = totalSpend + (complianceCosts || 0) + (equipmentCosts || 0); %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-5 mb-6">
      <h3 class="text-sm font-semibold text-gray-900 mb-4">Cost Breakdown by Source</h3>
      <div class="space-y-3">
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-2">
            <span class="w-2.5 h-2.5 rounded-full bg-brand-500"></span>
            <span class="text-sm text-gray-700">Manual Cost Entries</span>
          </div>
          <span class="text-sm font-semibold text-gray-900">$<%= totalSpend.toLocaleString() %></span>
        </div>
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-2">
            <span class="w-2.5 h-2.5 rounded-full bg-purple-500"></span>
            <a href="#compliance" onclick="activateTab(document.querySelector('a[href=\'#compliance\']'))" class="text-sm text-purple-700 hover:underline">Plans & Approvals (Council fees, TGS costs)</a>
          </div>
          <span class="text-sm font-semibold text-gray-900">$<%= (complianceCosts || 0).toLocaleString() %></span>
        </div>
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-2">
            <span class="w-2.5 h-2.5 rounded-full bg-amber-500"></span>
            <a href="#equipment" onclick="activateTab(document.querySelector('a[href=\'#equipment\']'))" class="text-sm text-amber-700 hover:underline">Equipment Maintenance</a>
          </div>
          <span class="text-sm font-semibold text-gray-900">$<%= (equipmentCosts || 0).toLocaleString() %></span>
        </div>
        <div class="border-t border-gray-200 pt-3 flex items-center justify-between">
          <span class="text-sm font-bold text-gray-900">Total All Sources</span>
          <span class="text-base font-bold text-gray-900">$<%= allSourcesTotal.toLocaleString() %></span>
        </div>
      </div>
    </div>

    <% if (costEntries.length > 0) { %>
    <h3 class="text-sm font-semibold text-gray-900 mb-3">Recent Cost Entries</h3>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-gray-200">
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Date</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Category</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Description</th>
            <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase">Supplier</th>
            <th class="px-4 py-3 text-right text-xs font-medium text-gray-500 uppercase">Amount</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% costEntries.forEach(ce => { %>
          <tr class="hover:bg-gray-50">
            <td class="px-4 py-3 text-gray-500 text-xs"><%= ce.entry_date ? formatDate(ce.entry_date) : '' %></td>
            <td class="px-4 py-3 text-gray-600"><%= ce.category ? ce.category.replace(/_/g, ' ') : '-' %></td>
            <td class="px-4 py-3 text-gray-600"><%= ce.description || '-' %></td>
            <td class="px-4 py-3 text-gray-500"><%= ce.supplier || '-' %></td>
            <td class="px-4 py-3 text-gray-900 text-right font-medium">$<%= (ce.amount || 0).toLocaleString() %></td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>
  <% } %>

  <!-- Accounts Tab (conditional) -->
  <% if (canViewAccounts) { %>
  <div id="accounts" class="tab-content">
    <div class="mb-4">
      <h2 class="text-lg font-semibold text-gray-900">Accounts</h2>
    </div>

    <!-- Accounts Status -->
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 mb-6">
      <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide mb-3">Accounts Status</h3>
      <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium
        <% if (job.accounts_status === 'on_track') { %>bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20
        <% } else if (job.accounts_status === 'overdue') { %>bg-red-50 text-red-700 ring-1 ring-red-600/20
        <% } else if (job.accounts_status === 'disputed') { %>bg-amber-50 text-amber-700 ring-1 ring-amber-600/20
        <% } else { %>bg-white text-gray-500<% } %>">
        <%= job.accounts_status ? job.accounts_status.replace('_', ' ') : 'N/A' %>
      </span>
    </div>

    <!-- Accounts Documents -->
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6">
      <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide mb-4">Accounts Documents</h3>
      <% if (accountsDocs.length === 0) { %>
        <p class="text-gray-400 text-sm">No accounts documents uploaded yet.</p>
      <% } else { %>
        <%
          // Group documents by category
          const accountsGrouped = {};
          accountsDocs.forEach(doc => {
            const cat = doc.category || 'Uncategorised';
            if (!accountsGrouped[cat]) accountsGrouped[cat] = [];
            accountsGrouped[cat].push(doc);
          });
        %>
        <div class="space-y-4">
          <% Object.keys(accountsGrouped).forEach(category => { %>
          <div>
            <h4 class="text-sm font-medium text-gray-600 mb-2 capitalize"><%= category %></h4>
            <ul class="space-y-1">
              <% accountsGrouped[category].forEach(doc => { %>
              <li class="flex items-center justify-between py-2 px-3 bg-white/50 rounded-lg">
                <div class="flex items-center gap-2">
                  <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
                  <span class="text-sm text-gray-600"><%= doc.title || doc.filename %></span>
                </div>
                <% if (doc.file_path) { %>
                  <a href="<%= doc.file_path %>" target="_blank" class="text-xs text-brand-600 hover:text-brand-700 hover:underline">View</a>
                <% } %>
              </li>
              <% }) %>
            </ul>
          </div>
          <% }) %>
        </div>
      <% } %>
    </div>
  </div>
  <% } %>

</div>

<!-- Tab switching script -->
<script>
  function activateTab(clicked) {
    // Update tab styling
    const tabs = clicked.parentElement.querySelectorAll('a');
    tabs.forEach(tab => {
      tab.classList.remove('text-brand-700', 'border-brand-500');
      tab.classList.add('text-gray-500', 'border-transparent');
    });
    clicked.classList.remove('text-gray-500', 'border-transparent');
    clicked.classList.add('text-brand-700', 'border-brand-500');

    // Show/hide content
    const targetId = clicked.getAttribute('href').substring(1);
    document.querySelectorAll('.tab-content').forEach(content => {
      content.style.display = 'none';
    });
    const target = document.getElementById(targetId);
    if (target) target.style.display = 'block';
  }

  // Initialise: activate first tab or hash-targeted tab
  document.addEventListener('DOMContentLoaded', function() {
    const hash = window.location.hash;
    const tabs = document.querySelectorAll('nav a[href^="#"]');
    if (hash && document.querySelector('a[href="' + hash + '"]')) {
      activateTab(document.querySelector('a[href="' + hash + '"]'));
    } else if (tabs.length > 0) {
      activateTab(tabs[0]);
    }
  });

  // Load unlinked compliance items for the "Link Existing" dropdowns
  var complianceLoaded = false;
  function loadUnlinkedCompliance() {
    if (complianceLoaded) return;
    complianceLoaded = true;
    fetch('/jobs/api/compliance/unlinked?job_id=<%= job.id %>', { headers: csrfHeaders() })
      .then(function(r) { return r.json(); })
      .then(function(items) {
        ['link-compliance-select', 'link-plan-select'].forEach(function(id) {
          var sel = document.getElementById(id);
          if (!sel) return;
          items.forEach(function(item) {
            var opt = new Option(item.title + (item.item_type ? ' (' + item.item_type.replace(/_/g, ' ') + ')' : ''), item.id);
            sel.add(opt);
          });
        });
      }).catch(function() {});
  }
  // Load when either link form is shown
  document.querySelectorAll('[onclick*="link-compliance-form"], [onclick*="link-plan-form"]').forEach(function(btn) {
    btn.addEventListener('click', loadUnlinkedCompliance);
  });
</script>
