<% locals.currentPage = 'bookings' %>

<!-- Sub-nav tab bar -->
<div class="bookings-subnav">
  <a href="/bookings?view=board" class="subnav-tab <%= currentView === 'board' ? 'active' : '' %>">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"/></svg>
    Board
  </a>
  <a href="/bookings?view=list" class="subnav-tab <%= currentView === 'list' ? 'active' : '' %>">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 10h16M4 14h16M4 18h16"/></svg>
    List
  </a>
  <a href="/bookings?view=calendar" class="subnav-tab <%= currentView === 'calendar' ? 'active' : '' %>">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
    Calendar
  </a>
  <a href="/bookings?view=map" class="subnav-tab <%= currentView === 'map' ? 'active' : '' %>">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
    Map
  </a>
  <a href="/bookings?view=archive" class="subnav-tab <%= currentView === 'archive' ? 'active' : '' %>">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4"/></svg>
    Archive
  </a>
  <a href="/bookings?view=requests" class="subnav-tab <%= currentView === 'requests' ? 'active' : '' %>">
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/></svg>
    Requests
  </a>
</div>

<!-- Page header -->
<div class="bookings-header">
  <div class="bookings-header-left">
    <h1 class="bookings-title">Bookings<br><span>Board</span></h1>
  </div>
  <div class="bookings-header-center">
    <div class="bookings-search-wrap">
      <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>
      <input type="text" id="bookings-search" class="bookings-search-input" placeholder="Search bookings..." value="<%= currentSearch %>" data-shortcut="/">
      <kbd class="bookings-search-kbd">/</kbd>
    </div>
  </div>
  <div class="bookings-header-right">
    <button class="bookings-action-btn" title="Export">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
      Export
    </button>
    <a href="/bookings/new" class="bookings-new-btn">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/></svg>
      New Booking
    </a>
  </div>
</div>

<!-- Status summary pills -->
<div class="bookings-status-bar">
  <a href="/bookings" class="status-pill <%= !currentStatus ? 'active' : '' %>">
    <span class="status-dot" style="background:#6B7280"></span>
    All <strong><%= stats.total %></strong>
  </a>
  <a href="/bookings?status=green_to_go" class="status-pill <%= currentStatus === 'green_to_go' ? 'active' : '' %>">
    <span class="status-dot" style="background:#22C55E"></span>
    Green to Go <strong><%= stats.greenToGo %></strong>
  </a>
  <a href="/bookings?status=confirmed" class="status-pill <%= currentStatus === 'confirmed' ? 'active' : '' %>">
    <span class="status-dot" style="background:#3B82F6"></span>
    Confirmed <strong><%= stats.confirmed %></strong>
  </a>
  <a href="/bookings?status=in_progress" class="status-pill <%= currentStatus === 'in_progress' ? 'active' : '' %>">
    <span class="status-dot" style="background:#F59E0B"></span>
    In Progress <strong><%= stats.inProgress %></strong>
  </a>
  <a href="/bookings?status=completed" class="status-pill <%= currentStatus === 'completed' ? 'active' : '' %>">
    <span class="status-dot" style="background:#8B5CF6"></span>
    Completed <strong><%= stats.completed %></strong>
  </a>
  <a href="/bookings?status=unconfirmed" class="status-pill <%= currentStatus === 'unconfirmed' ? 'active' : '' %>">
    <span class="status-dot" style="background:#EF4444"></span>
    Unconfirmed <strong><%= stats.unconfirmed %></strong>
  </a>
  <a href="/bookings?status=cancelled" class="status-pill <%= currentStatus === 'cancelled' ? 'active' : '' %>">
    <span class="status-dot" style="background:#9CA3AF"></span>
    Cancelled <strong><%= stats.cancelled %></strong>
  </a>
</div>

<!-- Filter bar -->
<div class="bookings-filter-bar" id="bookings-filter-bar">
  <div class="filter-row-main">
    <!-- Date Navigator -->
    <div class="date-navigator">
      <button id="date-prev" class="date-nav-arrow" title="Previous day">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
      </button>
      <div class="date-nav-label">
        <span class="date-nav-sub">Date, from 6 am</span>
        <input type="date" id="bookings-date" class="date-nav-input" value="<%= currentDate %>">
      </div>
      <button id="date-next" class="date-nav-arrow" title="Next day">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
      </button>
      <button id="date-today" class="date-today-btn">Today</button>
    </div>

    <!-- Depot filter -->
    <div class="filter-dropdown-wrap">
      <label class="filter-label">Depot</label>
      <select id="filter-depot" class="filter-select">
        <option value="">All Depots</option>
        <% depots.forEach(d => { %>
        <option value="<%= d %>" <%= currentDepot === d ? 'selected' : '' %>><%= d %></option>
        <% }) %>
      </select>
    </div>

    <!-- More filters toggle -->
    <button id="toggle-more-filters" class="filter-toggle-btn">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"/></svg>
      More Filters
      <svg class="w-3 h-3 chevron-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 9l-7 7-7-7"/></svg>
    </button>

    <!-- Deleted items -->
    <select class="filter-select filter-select-sm">
      <option>Hide Deleted</option>
      <option>Show Deleted</option>
      <option>Only Deleted</option>
    </select>
  </div>

  <!-- Expandable filter rows -->
  <div class="filter-expanded-rows" id="filter-expanded" style="display:none;">
    <div class="filter-grid">
      <div class="filter-chip-group">
        <label>Client</label>
        <select class="filter-select"><option>All Clients</option></select>
      </div>
      <div class="filter-chip-group">
        <label>Project</label>
        <select class="filter-select"><option>All Projects</option></select>
      </div>
      <div class="filter-chip-group">
        <label>Supplier</label>
        <select class="filter-select"><option>All Suppliers</option></select>
      </div>
      <div class="filter-chip-group">
        <label>Classification</label>
        <select class="filter-select"><option>All</option></select>
      </div>
      <div class="filter-chip-group">
        <label>Planning</label>
        <select class="filter-select"><option>All</option></select>
      </div>
      <div class="filter-chip-group">
        <label>Booking Status</label>
        <select class="filter-select"><option>All Statuses</option></select>
      </div>
      <div class="filter-chip-group">
        <label>Location</label>
        <select class="filter-select"><option>All Locations</option></select>
      </div>
      <div class="filter-chip-group">
        <label>People</label>
        <select class="filter-select"><option>All People</option></select>
      </div>
      <div class="filter-chip-group">
        <label>Billable</label>
        <select class="filter-select">
          <option>Billable & Non-billable</option>
          <option>Billable only</option>
          <option>Non-billable only</option>
        </select>
      </div>
      <div class="filter-chip-group">
        <label>Invoiced</label>
        <select class="filter-select">
          <option>Invoiced & Not Invoiced</option>
          <option>Invoiced only</option>
          <option>Not invoiced only</option>
        </select>
      </div>
    </div>
  </div>

  <!-- Sort controls -->
  <div class="sort-controls">
    <button class="sort-btn active">
      <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
      Sort by time
    </button>
    <button class="sort-btn">
      <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/></svg>
      Sort by depot
    </button>
    <button class="sort-btn">
      <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>
      Sort by client
    </button>
    <div class="sort-spacer"></div>
    <button class="sort-btn" id="toggle-resource-panel" title="Toggle resource panel">
      <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
      Resources
    </button>
  </div>
</div>

<% if (currentView === 'board') { %>
<!-- ============ BOARD VIEW ============ -->
<div class="bookings-board-wrap">
  <div class="bookings-board" id="bookings-board">
    <% if (bookings.length === 0) { %>
    <!-- Empty state -->
    <div class="bookings-empty">
      <div class="bookings-empty-icon">
        <svg class="w-10 h-10 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
      </div>
      <h3 class="text-lg font-semibold text-gray-500 mt-4">No bookings found</h3>
      <p class="text-sm text-gray-400 mt-1 max-w-sm">There are no bookings matching your current filters. Try adjusting your filters or create a new booking.</p>
      <div class="flex gap-3 mt-5">
        <a href="/bookings" class="px-4 py-2 text-sm font-medium text-gray-600 bg-gray-100 rounded-lg hover:bg-gray-200 transition">Clear Filters</a>
        <a href="/bookings/new" class="px-4 py-2 text-sm font-medium text-white bg-brand-600 rounded-lg hover:bg-brand-700 transition">New Booking</a>
      </div>
    </div>
    <% } else { %>
    <% bookings.forEach((booking, idx) => {
      const start = new Date(booking.startDateTime);
      const end = new Date(booking.endDateTime);
      const days = ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'];
      const dayName = days[start.getDay()];
      const startStr = `${dayName} ${start.getDate()}/${start.getMonth()+1} ${start.getHours()}:${String(start.getMinutes()).padStart(2,'0')}${start.getHours() < 12 ? 'am' : 'pm'}`;
      const endStr = `${end.getHours()}:${String(end.getMinutes()).padStart(2,'0')}${end.getHours() < 12 ? 'am' : 'pm'}`;
      const confirmedCount = booking.personnel.filter(p => p.confirmed).length;
      const totalPeople = booking.personnel.length;
    %>
    <div class="booking-card" data-id="<%= booking.id %>" data-status="<%= booking.status %>" style="animation-delay: <%= idx * 40 %>ms"
      ondragover="event.preventDefault(); this.style.outline='2px solid #2B7FFF'; this.style.outlineOffset='2px'"
      ondragleave="this.style.outline=''; this.style.outlineOffset=''"
      ondrop="handleCrewDrop(event, <%= booking.id %>)">
      <!-- Quick actions (hover) -->
      <div class="booking-card-actions">
        <button class="card-action-btn" title="Edit" onclick="event.stopPropagation(); openBookingDetail(<%= booking.id %>)">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/></svg>
        </button>
        <button class="card-action-btn" title="Duplicate" onclick="event.stopPropagation(); duplicateBooking(<%= booking.id %>)">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"/></svg>
        </button>
      </div>

      <!-- Compact header: status + ID + time -->
      <div class="booking-card-header status-<%= booking.status %>">
        <span class="booking-status-label"><%= booking.status.replace(/_/g, ' ').replace(/\b\w/g, l => l.toUpperCase()) %></span>
        <span class="booking-id">#<%= booking.id %></span>
      </div>

      <!-- Card body — clean and scannable -->
      <div class="booking-card-body" onclick="openBookingDetail(<%= booking.id %>)">
        <div class="booking-title-row">
          <span class="booking-project-title"><%= booking.project.name %></span>
        </div>

        <div class="booking-info-grid">
          <div class="booking-info-item">
            <svg class="w-3.5 h-3.5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            <span><%= startStr %> &ndash; <%= endStr %></span>
          </div>
          <div class="booking-info-item">
            <svg class="w-3.5 h-3.5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/></svg>
            <span><%= booking.project.client %></span>
          </div>
          <div class="booking-info-item">
            <svg class="w-3.5 h-3.5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
            <span><%= booking.project.address %></span>
          </div>
        </div>

        <!-- Vehicle plates (if any) -->
        <% if (booking.vehicles.length > 0) { %>
        <div class="booking-vehicles-list">
          <% booking.vehicles.forEach(function(v) { %>
          <div class="vehicle-plate-row">
            <svg class="w-3 h-3 text-purple-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16V6a1 1 0 00-1-1H4a1 1 0 00-1 1v10a1 1 0 001 1h1m8-1a1 1 0 01-1 1H9m4-1V8a1 1 0 011-1h2.586a1 1 0 01.707.293l3.414 3.414a1 1 0 01.293.707V16a1 1 0 01-1 1h-1m-6-1a1 1 0 001 1h1M5 17a2 2 0 104 0m-4 0a2 2 0 114 0m6 0a2 2 0 104 0m-4 0a2 2 0 114 0"/></svg>
            <span class="vehicle-plate-text"><%= v.registration ? v.registration.toUpperCase() : '' %> <%= v.name %></span>
          </div>
          <% }) %>
        </div>
        <% } %>

        <!-- Crew list with confirmation ticks -->
        <div class="booking-crew-list">
          <% if (booking.supervisor) { %>
          <div class="crew-member-row crew-supervisor">
            <svg class="w-3 h-3 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>
            <span class="crew-name font-medium"><%= booking.supervisor %></span>
            <svg class="w-3 h-3 text-emerald-500 ml-auto" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/></svg>
          </div>
          <% } %>
          <% booking.personnel.forEach(function(p) { %>
          <div class="crew-member-row">
            <span class="crew-role-badge"><%= p.role === 'traffic_controller' ? 'TC' : (p.role === 'leading_hand' ? 'LH' : (p.role === 'supervisor' ? 'SUP' : '')) %></span>
            <span class="crew-name"><%= p.name %></span>
            <% if (p.confirmed) { %>
              <svg class="w-3 h-3 text-emerald-500 ml-auto" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/></svg>
            <% } else { %>
              <svg class="w-3 h-3 text-gray-300 ml-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            <% } %>
          </div>
          <% }) %>
        </div>

        <!-- Qualification warnings -->
        <% const allWarnings = booking.personnel.filter(p => p.warnings && p.warnings.length > 0); %>
        <% if (allWarnings.length > 0) { %>
        <div class="booking-warnings">
          <% allWarnings.forEach(function(p) { p.warnings.forEach(function(w) { %>
          <div class="booking-warning-badge">
            <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 16.5c-.77.833.192 2.5 1.732 2.5z"/></svg>
            <%= w %>
          </div>
          <% }) }) %>
        </div>
        <% } %>

        <% if (booking.scheduleWarning) { %>
        <div class="booking-schedule-warning-inline">
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01"/></svg>
          <%= booking.scheduleWarning %>
        </div>
        <% } %>
      </div>

      <!-- Compact footer — single row with counts -->
      <div class="booking-card-footer-compact">
        <span class="footer-badge<%= booking.dockets > 0 ? ' has-count' : '' %>">
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
          <%= booking.dockets %>
        </span>
        <span class="footer-badge<%= booking.notes > 0 ? ' has-count' : '' %>">
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/></svg>
          <%= booking.notes %>
        </span>
        <span class="footer-badge<%= booking.tasks > 0 ? ' has-count' : '' %>">
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/></svg>
          <%= booking.tasks %>
        </span>
        <span class="footer-badge<%= booking.docs > 0 ? ' has-count' : '' %>">
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4"/></svg>
          <%= booking.docs %>
        </span>
        <span class="depot-tag"><%= booking.depot %></span>
      </div>
    </div>
    <% }) %>
    <% } %>
  </div>

  <!-- Right resource panel (togglable) -->
  <div class="resource-panel" id="resource-panel" style="display:none;">
    <div class="resource-panel-header">
      <div class="flex items-center gap-2">
        <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
        <h3>People</h3>
      </div>
      <button id="close-resource-panel" class="resource-close-btn">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>
    <div class="resource-panel-body">
      <!-- Show/Hide filter toggles (like screenshot) -->
      <div class="resource-show-hide">
        <button class="res-toggle-btn" data-filter="tl-only">Only TLs</button>
        <button class="res-toggle-btn" data-filter="assigned">Assigned</button>
        <button class="res-toggle-btn" data-filter="blocked">Blocked</button>
        <button class="res-toggle-btn" data-filter="on-leave">On Leave</button>
        <button class="res-toggle-btn" data-filter="can-drive">Can Drive</button>
        <button class="res-toggle-btn" data-filter="first-aid">Only First Aid</button>
      </div>

      <!-- Dropdowns -->
      <div class="resource-filter-row">
        <div class="resource-filter-group">
          <label>Specialisation</label>
          <select class="filter-select" id="res-filter-spec">
            <option value="">All</option>
            <option value="traffic_controller">Traffic Controller</option>
            <option value="leading_hand">Team Leader</option>
            <option value="supervisor">Supervisor</option>
            <option value="pilot_vehicle">Pilot Vehicle</option>
          </select>
        </div>
        <div class="resource-filter-group">
          <label>Depot</label>
          <select class="filter-select" id="res-filter-depot">
            <option value="">All Depots</option>
            <% depots.forEach(d => { %>
            <option value="<%= d %>"><%= d %></option>
            <% }) %>
          </select>
        </div>
      </div>
      <div class="resource-filter-row">
        <div class="resource-filter-group">
          <label>Employment Type</label>
          <select class="filter-select" id="res-filter-employment">
            <option value="">All</option>
            <option value="employee">Employee</option>
            <option value="casual">Casual</option>
            <option value="subcontractor">Subcontractor</option>
          </select>
        </div>
      </div>

      <!-- Distance pills -->
      <div class="resource-distance-filters">
        <label>Distance</label>
        <div class="distance-pills">
          <button class="distance-pill">5km</button>
          <button class="distance-pill">10km</button>
          <button class="distance-pill">20km</button>
          <button class="distance-pill">30km</button>
          <button class="distance-pill">50km</button>
          <button class="distance-pill active">All</button>
        </div>
      </div>

      <!-- Hours pills -->
      <div class="resource-distance-filters">
        <label>Hours</label>
        <div class="distance-pills">
          <button class="distance-pill">8 Hrs</button>
          <button class="distance-pill">16 Hrs</button>
          <button class="distance-pill">24 Hrs</button>
          <button class="distance-pill">32 Hrs</button>
          <button class="distance-pill">40 Hrs</button>
          <button class="distance-pill active">All</button>
        </div>
      </div>

      <!-- Search crew -->
      <div class="resource-search-wrap">
        <svg class="w-3.5 h-3.5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>
        <input type="text" id="resource-search" class="resource-search-input" placeholder="Search crew...">
      </div>

      <div class="resource-available-header">
        <span>Resources available from 6am to 6pm (day shift)</span>
        <span class="text-xs text-gray-400" id="resource-count-label">Loading...</span>
      </div>
      <div class="resource-list" id="resource-list">
        <div class="p-4 text-center text-gray-400 text-sm">Loading crew...</div>
      </div>
    </div>
  </div>
</div>

<% } else if (currentView === 'list') { %>
<!-- ============ LIST VIEW ============ -->
<div class="bookings-list-wrap">
  <div class="overflow-x-auto">
    <table class="bookings-table">
      <thead>
        <tr>
          <th class="w-8"></th>
          <th class="sortable">Job # <svg class="w-3 h-3 inline" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg></th>
          <th>Status</th>
          <th class="sortable">Start</th>
          <th>End</th>
          <th>Client</th>
          <th>Project</th>
          <th>Team Leader</th>
          <th>People</th>
          <th>Vehicles</th>
          <th>Address</th>
          <th>Order #</th>
        </tr>
      </thead>
      <tbody>
        <% bookings.forEach(booking => {
          const start = new Date(booking.startDateTime);
          const end = new Date(booking.endDateTime);
          const confirmedCount = booking.personnel.filter(p => p.confirmed).length;
        %>
        <tr class="booking-list-row" data-status="<%= booking.status %>" onclick="openBookingDetail(<%= booking.id %>)">
          <td>
            <div class="list-status-bar status-<%= booking.status %>"></div>
          </td>
          <td><span class="job-id-chip">#<%= booking.id %></span></td>
          <td>
            <span class="list-status-badge status-<%= booking.status %>">
              <%= booking.status.replace(/_/g, ' ').replace(/\b\w/g, l => l.toUpperCase()) %>
            </span>
          </td>
          <td class="whitespace-nowrap"><%= start.toLocaleDateString('en-AU', {day:'2-digit',month:'short'}) %> <%= String(start.getHours()).padStart(2,'0') %>:<%= String(start.getMinutes()).padStart(2,'0') %></td>
          <td class="whitespace-nowrap"><%= String(end.getHours()).padStart(2,'0') %>:<%= String(end.getMinutes()).padStart(2,'0') %></td>
          <td><%= booking.project.client %></td>
          <td class="max-w-xs truncate"><%= booking.project.name %></td>
          <td><%= booking.supervisor %></td>
          <td>
            <span class="<%= confirmedCount < booking.personnel.length ? 'text-amber-600 font-medium' : 'text-emerald-600' %>">
              <%= confirmedCount %>/<%= booking.personnel.length %>
            </span>
          </td>
          <td><%= booking.vehicles.length %></td>
          <td class="max-w-xs truncate text-gray-500"><%= booking.project.address %></td>
          <td><%= booking.project.orderNumber %></td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  </div>

  <!-- Pagination -->
  <div class="bookings-pagination">
    <div class="pagination-info">Showing 1-<%= bookings.length %> of <%= bookings.length %> bookings</div>
    <div class="pagination-controls">
      <button class="page-btn" disabled>First</button>
      <button class="page-btn" disabled>&lt; Prev</button>
      <button class="page-btn active">1</button>
      <button class="page-btn">Next &gt;</button>
      <button class="page-btn">Last</button>
    </div>
    <div class="pagination-size">
      <select class="filter-select filter-select-sm">
        <option>Show 25</option>
        <option>Show 50</option>
        <option selected>Show 100</option>
      </select>
    </div>
  </div>
</div>
<% } else if (currentView === 'calendar') { %>
<!-- ============ CALENDAR VIEW ============ -->
<%
  const calDate = new Date(currentDate + 'T00:00:00');
  const calYear = calDate.getFullYear();
  const calMonth = calDate.getMonth();
  const firstDay = new Date(calYear, calMonth, 1);
  const lastDay = new Date(calYear, calMonth + 1, 0);
  const startPad = firstDay.getDay(); // 0=Sun
  const totalDays = lastDay.getDate();
  const monthName = calDate.toLocaleDateString('en-AU', { month: 'long', year: 'numeric' });
  const prevMonth = new Date(calYear, calMonth - 1, 1).toISOString().split('T')[0];
  const nextMonth = new Date(calYear, calMonth + 1, 1).toISOString().split('T')[0];

  // Group bookings by date
  const bookingsByDate = {};
  bookings.forEach(bk => {
    const d = bk.startDateTime ? bk.startDateTime.split('T')[0] : '';
    if (d) { if (!bookingsByDate[d]) bookingsByDate[d] = []; bookingsByDate[d].push(bk); }
  });
%>
<div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-hidden">
  <!-- Calendar Header -->
  <div class="flex items-center justify-between px-5 py-4 border-b border-gray-100">
    <a href="/bookings?view=calendar&date=<%= prevMonth %>" class="p-2 hover:bg-gray-100 rounded-lg transition">
      <svg class="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
    </a>
    <h2 class="text-lg font-bold text-gray-900"><%= monthName %></h2>
    <a href="/bookings?view=calendar&date=<%= nextMonth %>" class="p-2 hover:bg-gray-100 rounded-lg transition">
      <svg class="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
    </a>
  </div>

  <!-- Day Headers -->
  <div class="grid grid-cols-7 border-b border-gray-100">
    <% ['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].forEach(d => { %>
      <div class="px-2 py-2 text-center text-[10px] font-bold text-gray-400 uppercase tracking-widest"><%= d %></div>
    <% }); %>
  </div>

  <!-- Calendar Grid -->
  <div class="grid grid-cols-7" id="calendar-grid">
    <% for (let i = 0; i < startPad; i++) { %>
      <div class="min-h-[100px] border-b border-r border-gray-50 bg-gray-50/50"></div>
    <% } %>
    <% for (let day = 1; day <= totalDays; day++) {
      const dateStr = calYear + '-' + String(calMonth + 1).padStart(2, '0') + '-' + String(day).padStart(2, '0');
      const today = new Date().toISOString().split('T')[0];
      const isToday = dateStr === today;
      const dayBookings = bookingsByDate[dateStr] || [];
      const statusColors = { unconfirmed: 'bg-red-400', confirmed: 'bg-blue-400', green_to_go: 'bg-emerald-400', in_progress: 'bg-amber-400', completed: 'bg-purple-400', cancelled: 'bg-gray-300', on_hold: 'bg-yellow-400' };
    %>
      <div class="min-h-[100px] border-b border-r border-gray-50 p-1 <%= isToday ? 'bg-brand-50/30' : '' %>"
        data-date="<%= dateStr %>"
        ondragover="event.preventDefault(); this.classList.add('bg-brand-50')"
        ondragleave="this.classList.remove('bg-brand-50')"
        ondrop="handleCalDrop(event, '<%= dateStr %>')">
        <div class="flex items-center justify-between mb-1">
          <a href="/bookings?view=board&date=<%= dateStr %>" class="text-xs font-semibold <%= isToday ? 'w-6 h-6 bg-brand-600 text-white rounded-full flex items-center justify-center' : 'text-gray-500 hover:text-brand-600' %>"><%= day %></a>
          <% if (dayBookings.length > 0) { %>
            <span class="text-[9px] font-bold text-gray-400"><%= dayBookings.length %></span>
          <% } %>
        </div>
        <div class="space-y-0.5">
          <% dayBookings.slice(0, 3).forEach(bk => {
            const t = new Date(bk.startDateTime);
            const timeStr = String(t.getHours()).padStart(2, '0') + ':' + String(t.getMinutes()).padStart(2, '0');
          %>
            <div class="text-[10px] leading-tight px-1 py-0.5 rounded cursor-pointer truncate hover:opacity-80 transition
              <%= statusColors[bk.status] || 'bg-gray-200' %> text-white font-medium"
              draggable="true"
              ondragstart="event.dataTransfer.setData('text/plain', '<%= bk.id %>')"
              onclick="window.location='/bookings/<%= bk.id %>'"
              title="<%= bk.project.name %> — <%= bk.supervisor || 'No supervisor' %>">
              <%= timeStr %> <%= bk.project.name ? bk.project.name.substring(0, 20) : 'BK-' + bk.id %>
            </div>
          <% }); %>
          <% if (dayBookings.length > 3) { %>
            <a href="/bookings?view=board&date=<%= dateStr %>" class="text-[9px] text-brand-600 font-medium pl-1">+<%= dayBookings.length - 3 %> more</a>
          <% } %>
        </div>
      </div>
    <% } %>
    <% const totalCells = startPad + totalDays; const remaining = 7 - (totalCells % 7); %>
    <% if (remaining < 7) { for (let i = 0; i < remaining; i++) { %>
      <div class="min-h-[100px] border-b border-r border-gray-50 bg-gray-50/50"></div>
    <% } } %>
  </div>
</div>

<script>
function handleCalDrop(event, targetDate) {
  event.preventDefault();
  event.currentTarget.classList.remove('bg-brand-50');
  const bookingId = event.dataTransfer.getData('text/plain');
  if (!bookingId) return;
  // Move booking to new date via API
  fetch('/bookings/' + bookingId + '/move', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'x-csrf-token': document.querySelector('meta[name="csrf-token"]')?.content || '' },
    body: 'new_date=' + encodeURIComponent(targetDate)
  }).then(r => {
    if (r.ok) window.location.reload();
    else alert('Failed to move booking');
  });
}
</script>

<% } else { %>
<!-- Other views placeholder -->
<div class="bookings-empty">
  <div class="bookings-empty-icon">
    <svg class="w-10 h-10 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"/></svg>
  </div>
  <h3 class="text-lg font-semibold text-gray-500 mt-4"><%= currentView.charAt(0).toUpperCase() + currentView.slice(1) %> View</h3>
  <p class="text-sm text-gray-400 mt-1">This view is coming soon. Use the Board or List view for now.</p>
</div>
<% } %>

<!-- Booking Detail Modal -->
<div class="booking-modal-backdrop" id="booking-modal-backdrop" style="display:none;">
  <div class="booking-modal" id="booking-modal">
    <div class="booking-modal-header">
      <h2 id="modal-title">Booking Details</h2>
      <button onclick="closeBookingDetail()" class="modal-close-btn">
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
      </button>
    </div>

    <!-- Modal tabs -->
    <div class="booking-modal-tabs">
      <button class="modal-tab active" data-tab="overview">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
        Overview
      </button>
      <button class="modal-tab" data-tab="resources">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
        Resources
      </button>
      <button class="modal-tab" data-tab="tasks">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"/></svg>
        Tasks
      </button>
      <button class="modal-tab" data-tab="dockets">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/></svg>
        Dockets
      </button>
      <button class="modal-tab" data-tab="notes">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/></svg>
        Notes
      </button>
      <button class="modal-tab" data-tab="history">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        History
      </button>
      <button class="modal-tab" data-tab="documents">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4"/></svg>
        Documents
      </button>
    </div>

    <!-- Status workflow -->
    <div class="booking-modal-status-flow" id="modal-status-flow">
      <!-- Filled by JS -->
    </div>

    <!-- Modal body (scrollable) -->
    <div class="booking-modal-body" id="modal-body">
      <!-- Loading skeleton -->
      <div class="modal-skeleton" id="modal-skeleton">
        <div class="skeleton-line w-3/4 h-5"></div>
        <div class="skeleton-line w-1/2 h-4 mt-3"></div>
        <div class="skeleton-line w-full h-20 mt-4"></div>
        <div class="skeleton-line w-full h-20 mt-3"></div>
      </div>
      <div id="modal-content" style="display:none;"></div>
    </div>

    <!-- Modal footer -->
    <div class="booking-modal-footer">
      <button class="modal-footer-btn secondary">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"/></svg>
        Clone
      </button>
      <button class="modal-footer-btn secondary">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z"/></svg>
        Print
      </button>
      <div class="flex-1"></div>
      <button class="modal-footer-btn primary" id="modal-save-btn">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
        Save
      </button>
    </div>
  </div>
</div>

<script>
// Bookings Board JS
(function() {
  // Search shortcut
  document.addEventListener('keydown', function(e) {
    if (e.key === '/' && !e.ctrlKey && !e.metaKey && document.activeElement.tagName !== 'INPUT' && document.activeElement.tagName !== 'TEXTAREA') {
      e.preventDefault();
      document.getElementById('bookings-search').focus();
    }
    if (e.key === 'Escape') {
      closeBookingDetail();
    }
  });

  // Search with debounce
  let searchTimeout;
  const searchInput = document.getElementById('bookings-search');
  if (searchInput) {
    searchInput.addEventListener('input', function() {
      clearTimeout(searchTimeout);
      searchTimeout = setTimeout(() => {
        const params = new URLSearchParams(window.location.search);
        if (this.value) params.set('search', this.value);
        else params.delete('search');
        window.location.href = '/bookings?' + params.toString();
      }, 500);
    });
  }

  // Date navigation
  const dateInput = document.getElementById('bookings-date');
  if (dateInput) {
    dateInput.addEventListener('change', function() {
      const params = new URLSearchParams(window.location.search);
      params.set('date', this.value);
      window.location.href = '/bookings?' + params.toString();
    });

    document.getElementById('date-prev')?.addEventListener('click', function() {
      const d = new Date(dateInput.value);
      d.setDate(d.getDate() - 1);
      dateInput.value = d.toISOString().split('T')[0];
      dateInput.dispatchEvent(new Event('change'));
    });

    document.getElementById('date-next')?.addEventListener('click', function() {
      const d = new Date(dateInput.value);
      d.setDate(d.getDate() + 1);
      dateInput.value = d.toISOString().split('T')[0];
      dateInput.dispatchEvent(new Event('change'));
    });

    document.getElementById('date-today')?.addEventListener('click', function() {
      dateInput.value = new Date().toISOString().split('T')[0];
      dateInput.dispatchEvent(new Event('change'));
    });
  }

  // Depot filter
  const depotSelect = document.getElementById('filter-depot');
  if (depotSelect) {
    depotSelect.addEventListener('change', function() {
      const params = new URLSearchParams(window.location.search);
      if (this.value) params.set('depot', this.value);
      else params.delete('depot');
      window.location.href = '/bookings?' + params.toString();
    });
  }

  // More filters toggle
  const toggleBtn = document.getElementById('toggle-more-filters');
  const expanded = document.getElementById('filter-expanded');
  if (toggleBtn && expanded) {
    toggleBtn.addEventListener('click', function() {
      const isOpen = expanded.style.display !== 'none';
      expanded.style.display = isOpen ? 'none' : 'block';
      this.classList.toggle('active', !isOpen);
      if (!isOpen) {
        expanded.style.animation = 'slideDown 0.25s ease-out';
      }
    });
  }

  // Resource panel toggle
  const resBtn = document.getElementById('toggle-resource-panel');
  const resPanel = document.getElementById('resource-panel');
  const resClose = document.getElementById('close-resource-panel');
  function loadResources() {
    const list = document.getElementById('resource-list');
    const label = document.getElementById('resource-count-label');
    if (!list) return;
    fetch('/bookings/resources?date=<%= currentDate %>')
      .then(r => r.json())
      .then(data => {
        label.textContent = data.available.length + ' available, ' + data.assigned.length + ' assigned';
        if (data.available.length === 0 && data.assigned.length === 0) {
          list.innerHTML = '<div class="p-4 text-center text-gray-400 text-sm">No crew members found</div>';
          return;
        }
        list.innerHTML = '';
        function renderItem(c, statusClass, statusLabel) {
          const initials = c.full_name.split(' ').map(n => n[0]).join('').substring(0, 2).toUpperCase();
          list.innerHTML += '<div class="resource-item" draggable="true" data-crew-id="' + c.id + '" data-crew-name="' + c.full_name + '" data-crew-role="' + (c.role||'') + '" style="cursor:grab" ondragstart="event.dataTransfer.setData(\'application/crew\', JSON.stringify({id:' + c.id + ',name:\'' + c.full_name.replace(/'/g, '') + '\',role:\'' + (c.role||'').replace(/'/g, '') + '\'}))">' +
            '<div class="resource-avatar">' + initials + '</div><div class="resource-info"><span class="resource-name">' + c.full_name + '</span><span class="resource-role">' + (c.role || '').replace(/_/g, ' ') + (c.employee_id ? ' · ' + c.employee_id : '') + '</span></div><span class="resource-status ' + statusClass + '">' + statusLabel + '</span></div>';
        }
        data.available.forEach(c => renderItem(c, 'available', 'Available'));
        data.assigned.forEach(c => renderItem(c, 'busy', 'On shift'));
      })
      .catch(() => { list.innerHTML = '<div class="p-4 text-center text-red-400 text-sm">Failed to load</div>'; });
  }
  if (resBtn && resPanel) {
    resBtn.addEventListener('click', function() {
      const isOpen = resPanel.style.display !== 'none';
      resPanel.style.display = isOpen ? 'none' : 'block';
      this.classList.toggle('active', !isOpen);
      if (!isOpen) loadResources();
    });
  }
  if (resClose && resPanel) {
    resClose.addEventListener('click', function() {
      resPanel.style.display = 'none';
      if (resBtn) resBtn.classList.remove('active');
    });
  }

  // Distance pill clicks
  document.querySelectorAll('.distance-pill').forEach(pill => {
    pill.addEventListener('click', function() {
      this.closest('.distance-pills').querySelectorAll('.distance-pill').forEach(p => p.classList.remove('active'));
      this.classList.add('active');
    });
  });

  // Resource panel search — filter crew list by name
  const resSearch = document.getElementById('resource-search');
  if (resSearch) {
    resSearch.addEventListener('input', function() {
      const q = this.value.toLowerCase();
      document.querySelectorAll('#resource-list .resource-item').forEach(item => {
        const name = (item.getAttribute('data-crew-name') || '').toLowerCase();
        item.style.display = name.includes(q) ? '' : 'none';
      });
    });
  }

  // Resource toggle buttons
  document.querySelectorAll('.res-toggle-btn').forEach(btn => {
    btn.addEventListener('click', function() { this.classList.toggle('active'); });
  });

  // Sort buttons
  document.querySelectorAll('.sort-btn:not(#toggle-resource-panel)').forEach(btn => {
    btn.addEventListener('click', function() {
      document.querySelectorAll('.sort-btn:not(#toggle-resource-panel)').forEach(b => b.classList.remove('active'));
      this.classList.add('active');
    });
  });

  // Modal tabs — switch visible content
  document.querySelectorAll('.modal-tab').forEach(tab => {
    tab.addEventListener('click', function() {
      document.querySelectorAll('.modal-tab').forEach(t => t.classList.remove('active'));
      this.classList.add('active');
      const tabName = this.getAttribute('data-tab');
      document.querySelectorAll('.modal-tab-content').forEach(c => c.style.display = 'none');
      const target = document.getElementById('tab-' + tabName);
      if (target) target.style.display = '';
    });
  });

  // Card entrance animation
  document.querySelectorAll('.booking-card').forEach(card => {
    card.style.opacity = '0';
    card.style.transform = 'translateY(12px)';
  });
  requestAnimationFrame(() => {
    document.querySelectorAll('.booking-card').forEach(card => {
      const delay = card.style.animationDelay || '0ms';
      setTimeout(() => {
        card.style.transition = 'opacity 0.35s ease, transform 0.35s ease';
        card.style.opacity = '1';
        card.style.transform = 'translateY(0)';
      }, parseInt(delay));
    });
  });
})();

// Drag-and-drop: assign crew from resource panel to booking card
function handleCrewDrop(event, bookingId) {
  event.preventDefault();
  event.currentTarget.style.outline = '';
  event.currentTarget.style.outlineOffset = '';

  const crewData = event.dataTransfer.getData('application/crew');
  if (!crewData) return;

  try {
    const crew = JSON.parse(crewData);
    fetch('/bookings/' + bookingId + '/crew', {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'x-csrf-token': document.querySelector('meta[name="csrf-token"]')?.content || '' },
      body: 'crew_member_id=' + crew.id + '&role_on_site=' + encodeURIComponent(crew.role.replace(/_/g, ' '))
    }).then(r => {
      if (r.redirected || r.ok) {
        // Show toast and reload
        const toast = document.createElement('div');
        toast.className = 'booking-toast show';
        toast.innerHTML = '<svg class="w-4 h-4 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg> ' + crew.name + ' assigned';
        document.body.appendChild(toast);
        setTimeout(() => { toast.remove(); window.location.reload(); }, 1200);
      }
    });
  } catch(e) { console.error('Drop error:', e); }
}

function openBookingDetail(id) {
  const backdrop = document.getElementById('booking-modal-backdrop');
  const modal = document.getElementById('booking-modal');
  const skeleton = document.getElementById('modal-skeleton');
  const content = document.getElementById('modal-content');
  const titleEl = document.getElementById('modal-title');
  const statusFlow = document.getElementById('modal-status-flow');

  backdrop.style.display = 'flex';
  document.body.style.overflow = 'hidden';
  skeleton.style.display = 'block';
  content.style.display = 'none';

  setTimeout(() => {
    backdrop.classList.add('open');
    modal.classList.add('open');
  }, 10);

  // Fetch booking data
  fetch('/bookings/' + id, { headers: { 'Accept': 'application/json' } })
    .then(r => r.json())
    .then(booking => {
      titleEl.textContent = 'Job ' + booking.id + ': ' + booking.project.name;

      // Status flow
      const statuses = ['Client Bk.', 'Unconf.', 'Confirmed', 'Locked', 'Conflict', 'G T G', 'Complete', 'Finalised'];
      const statusMap = { unconfirmed: 1, confirmed: 2, in_progress: 5, green_to_go: 5, completed: 6, cancelled: -1 };
      const activeIdx = statusMap[booking.status] || 0;
      statusFlow.innerHTML = statuses.map((s, i) => `<button class="status-flow-step ${i === activeIdx ? 'active' : ''} ${i < activeIdx ? 'done' : ''}">${s}</button>`).join('');

      const start = new Date(booking.startDateTime);
      const end = new Date(booking.endDateTime);

      content.innerHTML = `
        <div class="modal-section">
          <h4 class="modal-section-title">BOOKING DEPOT</h4>
          <div class="modal-field-grid">
            <div class="modal-field">
              <label>Booking depot</label>
              <p>${booking.depot}</p>
            </div>
            <div class="modal-field">
              <label>Supplier</label>
              <p>-</p>
            </div>
          </div>
        </div>

        <div class="modal-section highlight">
          <h4 class="modal-section-title">CLIENT INFORMATION</h4>
          <div class="modal-field-grid">
            <div class="modal-field">
              <label>Client</label>
              <p class="font-medium">${booking.project.client}</p>
            </div>
            <div class="modal-field">
              <label>Location Context</label>
              <p>-</p>
            </div>
          </div>
          <div class="modal-field-grid">
            <div class="modal-field">
              <label>Project</label>
              <p class="text-brand-600 font-medium">${booking.project.name}</p>
            </div>
          </div>
          <div class="modal-field-grid">
            <div class="modal-field">
              <label>Purchase Order / Client Job Number</label>
              <p>${booking.project.orderNumber}</p>
            </div>
            <div class="modal-field">
              <label>Client Billing Code</label>
              <p>${booking.project.billingCode}</p>
            </div>
          </div>
          <div class="modal-field-grid">
            <div class="modal-field">
              <label>Requester</label>
              <p>-</p>
            </div>
            <div class="modal-field">
              <label>Planner</label>
              <p>-</p>
            </div>
          </div>
          <div class="modal-field">
            <label>Site Contacts</label>
            <div class="modal-contact-pill">${booking.supervisor}</div>
          </div>
        </div>

        <div class="modal-section">
          <h4 class="modal-section-title">BOOKING INFORMATION</h4>
          <div class="modal-field-grid">
            <div class="modal-field">
              <label>Start Time</label>
              <p>${start.toLocaleDateString('en-AU')} ${String(start.getHours()).padStart(2,'0')}:${String(start.getMinutes()).padStart(2,'0')}</p>
            </div>
            <div class="modal-field">
              <label>Approximate End Time</label>
              <p class="text-amber-600 font-medium">${end.toLocaleDateString('en-AU')} ${String(end.getHours()).padStart(2,'0')}:${String(end.getMinutes()).padStart(2,'0')}</p>
            </div>
          </div>
          <div class="modal-field-grid">
            <div class="modal-field">
              <label>Depot Meeting Time</label>
              <p>${booking.depotMeetingTime || '-'}</p>
            </div>
            <div class="modal-field">
              <label>Straight-to-Site Time</label>
              <p>${booking.straightToSiteTime || '-'}</p>
            </div>
          </div>
          <div class="modal-field">
            <label>Booking Title</label>
            <p class="font-medium">${booking.title}</p>
          </div>
          ${booking.bookingDescription ? `
          <div class="modal-field">
            <label>Booking Description</label>
            <p class="whitespace-pre-line text-gray-600">${booking.bookingDescription}</p>
          </div>` : ''}
          <div class="modal-field">
            <label>Booking Tags</label>
            <p class="text-gray-400">No tags</p>
          </div>
        </div>

        <div class="modal-section">
          <h4 class="modal-section-title">LOCATION</h4>
          <div class="modal-field">
            <label>Main Address</label>
            <p>${booking.project.address}</p>
          </div>
        </div>

        <div class="modal-section">
          <h4 class="modal-section-title">PERSONNEL (${booking.personnel.length})</h4>
          <div class="modal-personnel-list">
            ${booking.personnel.map(p => `
              <div class="modal-personnel-row">
                <span class="modal-personnel-role">${p.role === 'Team Leader' ? 'TL' : 'TC'}</span>
                <span class="modal-personnel-name">${p.name}</span>
                ${p.confirmed
                  ? '<svg class="w-5 h-5 text-emerald-500" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 15l-5-5 1.41-1.41L11 14.17l7.59-7.59L20 8l-9 9z"/></svg>'
                  : '<svg class="w-5 h-5 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>'}
              </div>
              ${p.qualWarning ? '<div class="modal-personnel-warning">' + p.qualWarning + '</div>' : ''}
            `).join('')}
          </div>
        </div>
      `;

      // Add tab wrapper and other tab content
      const overviewHtml = content.innerHTML;
      const bkId = booking.id;

      // Build Resources tab
      const crewHtml = (booking.crew || []).map(c => `
        <div class="flex items-center gap-3 p-2 rounded-lg bg-gray-50 mb-1">
          <div class="w-7 h-7 rounded-full bg-brand-100 text-brand-700 flex items-center justify-center text-[10px] font-bold">${(c.full_name||'?').split(' ').map(n=>n[0]).join('').substring(0,2)}</div>
          <div class="flex-1"><span class="text-sm font-medium">${c.full_name||'Unknown'}</span> <span class="text-xs text-gray-400">${c.role_on_site||c.crew_role||''}</span></div>
          <span class="text-[10px] font-medium ${c.status==='confirmed'?'text-emerald-600':'text-amber-600'}">${c.status||'assigned'}</span>
        </div>
      `).join('') || '<p class="text-sm text-gray-400">No crew assigned yet.</p>';

      // Build Notes tab
      const notesHtml = (booking.allNotes || []).map(n => `
        <div class="p-3 rounded-lg bg-gray-50 border border-gray-100 mb-2">
          <div class="flex justify-between mb-1"><span class="text-xs font-medium text-gray-600">${n.author_name||'System'}</span><span class="text-[10px] text-gray-400">${n.created_at ? new Date(n.created_at).toLocaleDateString('en-AU',{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'}) : ''}</span></div>
          <p class="text-sm text-gray-700">${(n.content||'').replace(/</g,'&lt;')}</p>
        </div>
      `).join('') || '<p class="text-sm text-gray-400">No notes.</p>';

      // Build Dockets tab
      const docketsHtml = (booking.dockets || []).length > 0
        ? booking.dockets.map(d => `<a href="/bookings/${bkId}/dockets/${d.id}" class="block p-3 rounded-lg bg-gray-50 hover:bg-gray-100 mb-2 transition"><span class="text-sm font-medium">${d.docket_number}</span> <span class="inline-flex items-center px-2 py-0.5 rounded-full text-[10px] font-medium bg-gray-100 text-gray-600 ml-2">${(d.status||'draft').replace(/_/g,' ')}</span></a>`).join('')
        : '<p class="text-sm text-gray-400">No dockets yet. <a href="/bookings/' + bkId + '" class="text-brand-600 hover:underline">Go to booking</a> to create one.</p>';

      // Build Documents tab
      const docsHtml = (booking.documents || []).length > 0
        ? booking.documents.map(d => `<div class="flex items-center gap-2 p-2 rounded-lg bg-gray-50 mb-1"><svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z"/></svg><a href="/bookings/${bkId}/documents/${d.id}/download" class="text-sm font-medium text-brand-600 hover:text-brand-700">${d.title||d.original_name}</a></div>`).join('')
        : '<p class="text-sm text-gray-400">No documents.</p>';

      // Build History tab
      const historyHtml = (booking.activity || []).length > 0
        ? booking.activity.map(a => `<div class="flex items-start gap-3 mb-3"><div class="w-5 h-5 rounded-full bg-gray-100 flex items-center justify-center flex-shrink-0 mt-0.5"><svg class="w-3 h-3 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg></div><div><p class="text-sm text-gray-700">${a.details||a.action}</p><p class="text-[10px] text-gray-400">${a.user_name||'System'} · ${a.created_at ? new Date(a.created_at).toLocaleDateString('en-AU',{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'}) : ''}</p></div></div>`).join('')
        : '<p class="text-sm text-gray-400">No activity recorded.</p>';

      content.innerHTML = `
        <div class="modal-tab-content" id="tab-overview">${overviewHtml}</div>
        <div class="modal-tab-content" id="tab-resources" style="display:none"><div class="modal-section"><h4 class="modal-section-title">CREW (${(booking.crew||[]).length})</h4>${crewHtml}</div></div>
        <div class="modal-tab-content" id="tab-tasks" style="display:none"><div class="modal-section"><p class="text-sm text-gray-400">Tasks linked to this booking will appear here.</p></div></div>
        <div class="modal-tab-content" id="tab-dockets" style="display:none"><div class="modal-section"><h4 class="modal-section-title">DOCKETS</h4>${docketsHtml}</div></div>
        <div class="modal-tab-content" id="tab-notes" style="display:none"><div class="modal-section"><h4 class="modal-section-title">NOTES</h4>${notesHtml}</div></div>
        <div class="modal-tab-content" id="tab-history" style="display:none"><div class="modal-section"><h4 class="modal-section-title">ACTIVITY LOG</h4>${historyHtml}</div></div>
        <div class="modal-tab-content" id="tab-documents" style="display:none"><div class="modal-section"><h4 class="modal-section-title">DOCUMENTS</h4>${docsHtml}</div></div>
      `;

      // Reset to overview tab
      document.querySelectorAll('.modal-tab').forEach(t => t.classList.remove('active'));
      document.querySelector('.modal-tab[data-tab="overview"]').classList.add('active');

      skeleton.style.display = 'none';
      content.style.display = 'block';
    })
    .catch(err => {
      console.error('Failed to load booking:', err);
      skeleton.innerHTML = '<p class="text-red-500 text-sm">Failed to load booking details.</p>';
    });
}

function closeBookingDetail() {
  const backdrop = document.getElementById('booking-modal-backdrop');
  const modal = document.getElementById('booking-modal');
  if (!backdrop || backdrop.style.display === 'none') return;
  backdrop.classList.remove('open');
  modal.classList.remove('open');
  setTimeout(() => {
    backdrop.style.display = 'none';
    document.body.style.overflow = '';
  }, 250);
}

function duplicateBooking(id) {
  // Show a brief toast confirming duplication
  const toast = document.createElement('div');
  toast.className = 'booking-toast';
  toast.innerHTML = '<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"/></svg> Booking #' + id + ' duplicated';
  document.body.appendChild(toast);
  requestAnimationFrame(() => toast.classList.add('show'));
  setTimeout(() => {
    toast.classList.remove('show');
    setTimeout(() => toast.remove(), 300);
  }, 2000);
}

// Click backdrop to close
document.getElementById('booking-modal-backdrop')?.addEventListener('click', function(e) {
  if (e.target === this) closeBookingDetail();
});
</script>
