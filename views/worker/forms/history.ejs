<div class="space-y-5">
  <a href="/w/forms" class="inline-flex items-center text-sm text-brand-600 font-medium">
    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
    Back
  </a>
  <h1 class="text-xl font-bold text-gray-900">Form History</h1>

  <% if (forms.length === 0) { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm p-6 text-center">
      <p class="text-gray-500 text-sm">No forms submitted yet</p>
    </div>
  <% } else { %>
    <div class="bg-white border border-gray-200 rounded-xl shadow-sm divide-y divide-gray-100">
      <% forms.forEach(f => { %>
        <% const typeColors = { prestart: 'bg-emerald-50 text-emerald-700', take5: 'bg-brand-50 text-brand-700', incident: 'bg-red-50 text-red-700', hazard: 'bg-amber-50 text-amber-700', equipment: 'bg-blue-50 text-blue-700' }; %>
        <div class="p-3">
          <div class="flex items-center justify-between mb-1">
            <span class="inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-semibold <%= typeColors[f.form_type] || 'bg-gray-100 text-gray-600' %>">
              <%= f.form_type.charAt(0).toUpperCase() + f.form_type.slice(1).replace('5', ' 5') %>
            </span>
            <span class="inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-semibold
              <%= f.status === 'reviewed' ? 'bg-emerald-50 text-emerald-700' : f.status === 'submitted' ? 'bg-brand-50 text-brand-700' : 'bg-gray-100 text-gray-500' %>">
              <%= f.status.charAt(0).toUpperCase() + f.status.slice(1) %>
            </span>
          </div>
          <% if (f.client || f.job_number) { %>
            <p class="text-sm text-gray-900"><%= f.client || f.job_number %></p>
          <% } %>
          <p class="text-xs text-gray-500"><%= new Date(f.created_at).toLocaleString('en-AU', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %></p>
        </div>
      <% }) %>
    </div>
  <% } %>
</div>
